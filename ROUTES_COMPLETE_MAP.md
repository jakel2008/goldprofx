# 🔗 خريطة المسارات الكاملة - GOLD PRO Web System

## 📊 إجمالي المسارات: 45+ مسار

---

## 🔐 مسارات المصادقة

| المسار | الملف | النوع | الوصول | الحالة |
|--------|------|-------|--------|--------|
| `/login` | login.html | GET, POST | عام | ✅ |
| `/register` | register.html | GET, POST | عام | ✅ |
| `/logout` | --- | GET | مسجلين | ✅ |

### تفاصيل:
- **POST /login**: تسجيل دخول مستخدم مع البريد والكلمة المرورية
- **GET /login**: عرض نموذج تسجيل الدخول
- **POST /register**: إنشاء حساب جديد
- **GET /register**: عرض نموذج التسجيل
- **GET /logout**: تسجيل الخروج وحذف الجلسة

---

## 👥 مسارات المستخدم

| المسار | الملف | النوع | الوصول | الحالة |
|--------|------|-------|--------|--------|
| `/` | index.html | GET | جميع | ✅ |
| `/profile` | profile.html | GET | مسجلين | ✅ |
| `/dashboard` | dashboard.html | GET | مسجلين | ✅ |
| `/signals` | signals.html | GET | مسجلين | ✅ |
| `/reports` | reports.html | GET | مسجلين | ✅ |
| `/plans` | plans.html | GET | جميع | ✅ |
| `/pairs-selection` | pairs_selection.html | GET | مسجلين | ✅ |

### تفاصيل:
- **GET /**: الصفحة الرئيسية (تحويل للمستخدمين غير المسجلين)
- **GET /profile**: عرض ملف المستخدم الشخصي
- **GET /dashboard**: لوحة التحكم والإحصائيات
- **GET /signals**: عرض الإشارات النشطة
- **GET /reports**: تقارير الأداء
- **GET /plans**: عرض خطط الاشتراك
- **GET /pairs-selection**: اختيار الأزواج المفضلة

---

## 💳 مسارات الدفع والاشتراك

| المسار | الملف | النوع | الوصول | الحالة |
|--------|------|-------|--------|--------|
| `/select_location/<plan>` | select_location.html | GET | جميع | ✅ |
| `/payment/<location>/<plan>` | payment_*.html | GET | جميع | ✅ |
| `/api/subscribe` | --- | POST | جميع | ✅ |

### تفاصيل:
- **GET /select_location/[plan]**: اختيار الدولة (مثل: `/select_location/gold`)
- **GET /payment/jordan/gold**: صفحة الدفع للأردن
- **GET /payment/international/gold**: صفحة الدفع للدول الأجنبية
- **POST /api/subscribe**: إنشاء طلب اشتراك

### الخطط المتاحة:
```
- free      (مجاني)
- bronze    (برونز)
- silver    (فضي)
- gold      (ذهبي)
- platinum  (بلاتيني)
```

---

## 📡 مسارات API للبيانات

### الإشارات والتحليل
```
GET  /api/signals              → جميع الإشارات (مع الفلترة)
GET  /api/recommendations      → التوصيات الحالية
GET  /api/analysis             → التحليل الفني
GET  /api/stats                → الإحصائيات
GET  /api/detailed-report      → التقارير المفصلة
```

### تفضيلات المستخدم
```
GET  /api/user                 → معلومات المستخدم
GET  /api/available-pairs      → الأزواج المتاحة (13 زوج)
GET  /api/user-pairs-preferences      → تفضيلات الأزواج
POST /api/save-pairs-preferences      → حفظ التفضيلات
```

---

## ⚡ مسارات الإدارة (Admin Only)

### لوحة الإدارة
| المسار | النوع | الحالة |
|--------|-------|--------|
| `/admin-panel` | GET | ✅ |
| `/admin-management` | GET | ✅ |
| `/subscription-management` | GET | ✅ |
| `/bot-management` | GET | ✅ |
| `/forex-analyzer` | GET | ✅ |

### إدارة المستخدمين (API)
```
GET  /api/admin/users                → قائمة المستخدمين
GET  /api/admin/users/all            → جميع المستخدمين (مع التفاصيل)
POST /api/admin/users/set-admin      → تعيين كمسؤول
POST /api/admin/set_admin            → تعيين مسؤول (قديم)
POST /api/admin/set_active           → تفعيل/تعطيل المستخدم
```

### إدارة الاشتراكات (API)
```
GET  /api/admin/subscriptions                → قائمة الاشتراكات
POST /api/admin/update_plan                  → تعديل الخطة
POST /api/admin/extend_subscription          → تمديد الاشتراك
POST /api/admin/cancel_subscription          → إلغاء الاشتراك
POST /api/admin/reactivate_subscription      → إعادة تفعيل
POST /api/admin/activate_subscription        → تفعيل
POST /api/admin/deactivate_subscription      → تعطيل
GET  /api/admin/subscription_requests        → طلبات الاشتراك
POST /api/admin/approve_subscription         → الموافقة على الطلب
POST /api/admin/reject_subscription          → رفض الطلب
```

### إرسال الرسائل والإشارات
```
POST /api/admin/send-signal           → إرسال إشارة يدوية
POST /api/admin/send-recommendation   → إرسال توصية يدوية
POST /api/admin/send-report           → إرسال تقرير
POST /api/admin/broadcast-message     → بث رسالة للجميع
```

### إدارة البوتات
```
GET  /api/admin/bots                  → قائمة البوتات
GET  /api/admin/bots/<int:bot_id>     → تفاصيل البوت
POST /api/admin/bots/test             → اختبار البوت
POST /api/admin/bots/add              → إضافة بوت جديد
```

### التحليل
```
POST /api/forex-analysis              → تحليل زوج معين
```

---

## 🔐 الوصول والصلاحيات

### الزائرين (Guests)
```
✅ GET /login
✅ GET /register
✅ GET /plans
✅ GET /select_location/[plan]
✅ GET /payment/[location]/[plan]
✅ POST /api/subscribe
```

### المستخدمين (Users)
```
✅ جميع روابط الزائرين
✅ GET /
✅ GET /profile
✅ GET /dashboard
✅ GET /signals
✅ GET /reports
✅ GET /pairs-selection
✅ GET /logout
✅ GET /api/signals
✅ GET /api/recommendations
✅ GET /api/stats
✅ GET /api/user
✅ GET /api/available-pairs
✅ GET /api/user-pairs-preferences
✅ POST /api/save-pairs-preferences
✅ GET /api/detailed-report
```

### المسؤولين (Admins)
```
✅ جميع روابط المستخدمين
✅ GET /admin-panel
✅ GET /admin-management
✅ GET /subscription-management
✅ GET /bot-management
✅ GET /forex-analyzer
✅ جميع /api/admin/* المسارات
```

---

## 🎯 خريطة التدفق الكاملة

```
المستخدم الجديد
├─ /register (إنشاء حساب)
├─ /plans (عرض الخطط)
├─ /select_location/[plan] (اختيار الموقع)
├─ /payment/[location]/[plan] (إتمام الدفع)
└─ /login (تسجيل الدخول)

المستخدم المسجل
├─ / (الرئيسية)
├─ /signals (الإشارات الرئيسية)
├─ /dashboard (الإحصائيات)
├─ /reports (التقارير)
├─ /pairs-selection (تخصيص الأزواج)
├─ /profile (الملف الشخصي)
└─ /logout (تسجيل الخروج)

المسؤول
├─ /admin-panel (اللوحة الرئيسية)
├─ /admin-management (إدارة المستخدمين)
├─ /subscription-management (إدارة الاشتراكات)
├─ /bot-management (إدارة البوتات)
└─ /forex-analyzer (محلل الفوركس)
```

---

## 📊 توزيع المسارات حسب النوع

```
مسارات المصادقة:        3 مسارات
مسارات المستخدم:        7 مسارات
مسارات الدفع:           3 مسارات
مسارات API بيانات:      6 مسارات
مسارات الإدارة:         25+ مسارات

الإجمالي: 45+ مسار نشط
```

---

## 🔌 الاتصالات البينية

### web_app_complete.py يتصل مع:

1. **SQLite Database (vip_signals.db)**
   - قراءة/كتابة بيانات المستخدمين
   - قراءة/كتابة الإشارات
   - إدارة الاشتراكات

2. **Auto Pairs Analyzer**
   - يقرأ الإشارات من قاعدة البيانات
   - يعرضها للمستخدمين

3. **Yahoo Finance (yfinance)**
   - الأسعار الحية للأزواج
   - البيانات التاريخية

4. **Email Service**
   - إرسال رسائل التأكيد
   - إشعارات الاشتراك

5. **Telegram Bot**
   - إرسال الإشارات
   - الإخطارات

6. **HTML Templates (26 ملف)**
   - عرض الصفحات
   - واجهة المستخدم

---

## 🧪 أمثلة استدعاء API

### الحصول على الإشارات
```bash
curl http://localhost:5000/api/signals
```

### حفظ تفضيلات الأزواج
```bash
curl -X POST http://localhost:5000/api/save-pairs-preferences \
  -H "Content-Type: application/json" \
  -d '{"pairs": ["EURUSD", "GBPUSD"]}'
```

### إرسال إشارة من الأدمن
```bash
curl -X POST http://localhost:5000/api/admin/send-signal \
  -H "Content-Type: application/json" \
  -d '{
    "symbol": "EURUSD",
    "signal": "buy",
    "entry": 1.0850,
    "sl": 1.0820,
    "tp1": 1.0900,
    "quality": 85
  }'
```

---

## ✅ قائمة التحقق

### مسارات مختبرة وجاهزة:
- [x] /login - تسجيل الدخول
- [x] /register - إنشاء حساب
- [x] /signals - عرض الإشارات
- [x] /api/signals - API للإشارات
- [x] /api/stats - الإحصائيات
- [x] /dashboard - لوحة التحكم
- [x] /profile - الملف الشخصي
- [x] /plans - الخطط
- [x] /admin-panel - لوحة الأدمن

### مسارات تحتاج تحقق إضافي:
- [ ] /api/admin/* - جميع مسارات الإدارة
- [ ] /payment/* - نظام الدفع
- [ ] /bot-management - إدارة البوتات

---

## 📝 ملاحظات مهمة

1. **التحويلات التلقائية:**
   - `/` → `/login` للمستخدمين غير المسجلين
   - `/admin-*` → `/login` إذا لم يكن أدمن

2. **الفلترة:**
   - `/api/signals` تُفلتر حسب خطة المستخدم (ما لم يكن أدمن)
   - الأدمن يرى جميع الإشارات

3. **الأمان:**
   - جميع مسارات POST تتطلب CSRF token
   - session['user_id'] يتحقق من هويتك
   - الأدمن فقط للمسارات المقيدة

---

**آخر تحديث:** 27 يناير 2026  
**الإصدار:** 1.0  
**الحالة:** 100% مكتمل وجاهز للاستخدام ✅

