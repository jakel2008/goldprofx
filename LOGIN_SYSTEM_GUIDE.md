# 📝 دليل نظام التسجيل والدخول
## Login and Registration System Guide

---

## 📋 نظرة عامة
تم تطوير نظام تسجيل دخول وتسجيل شامل للتطبيق يوفر:

### ✨ المميزات الرئيسية
- ✅ **تسجيل مستخدمين جدد** - نموذج تسجيل آمن مع التحقق من صحة البيانات
- ✅ **تسجيل دخول آمن** - كلمات مرور مشفرة بـ PBKDF2
- ✅ **إدارة الجلسات** - جلسات تنتهي بعد 30 يوم
- ✅ **ملف شخصي للمستخدم** - لوحة تحكم شخصية لكل مستخدم
- ✅ **إدارة الخطط** - تحديث خطة الاشتراك للمستخدمين
- ✅ **تتبع النشاط** - تسجيل جميع أنشطة المستخدم

---

## 🗂️ ملفات النظام

### 1. **user_manager.py** 
نظام إدارة المستخدمين الكامل

#### الفئات الرئيسية:
```python
class UserManager:
    - init_database()         # إنشاء جداول قاعدة البيانات
    - register_user()         # تسجيل مستخدم جديد
    - login_user()           # تسجيل دخول المستخدم
    - verify_session()       # التحقق من صحة الجلسة
    - logout_user()          # تسجيل خروج المستخدم
    - get_user_info()        # الحصول على بيانات المستخدم
    - update_user_plan()     # تحديث خطة المستخدم
    - log_activity()         # تسجيل نشاط المستخدم
```

#### جداول قاعدة البيانات:
- **users** - بيانات المستخدمين (اسم، بريد، كلمة مرور، خطة، إلخ)
- **user_sessions** - جلسات المستخدمين النشطة
- **user_activity** - تسجيل أنشطة المستخدمين

### 2. **web_app.py**
التطبيق الرئيسي مع مسارات جديدة

#### المسارات الجديدة:
```
GET  /login              - صفحة تسجيل الدخول
POST /login              - معالجة تسجيل الدخول
GET  /register           - صفحة التسجيل
POST /register           - معالجة التسجيل
GET  /logout             - تسجيل الخروج
GET  /profile            - الملف الشخصي (محمي)
GET  /api/user           - بيانات المستخدم الحالي (API)
POST /api/subscribe      - تحديث الخطة (API)
```

### 3. **القوالب الجديدة**

#### login.html
- نموذج تسجيل دخول جميل وآمن
- عرض فوائد الحساب
- رابط للتسجيل الجديد

#### register.html
- نموذج تسجيل شامل
- التحقق من كلمات المرور
- الموافقة على الشروط والأحكام
- رابط لتسجيل الدخول

#### profile.html
- لوحة تحكم شخصية
- عرض المعلومات الشخصية
- إدارة الخطة والاشتراك
- إحصائيات المستخدم
- إعدادات الأمان

#### 404.html و 500.html
- صفحات أخطاء جميلة وودية

---

## 🔐 الأمان

### تشفير كلمات المرور
```python
# استخدام PBKDF2 مع 100,000 تكرار
hash = pbkdf2_hmac('sha256', password, salt, 100000)
```

### جلسات آمنة
```python
# توكنات عشوائية آمنة
session_token = secrets.token_urlsafe(32)

# انتهاء الجلسات بعد 30 يوم
expires_at = datetime.now() + timedelta(days=30)
```

### Decorator للحماية
```python
@login_required
def protected_route():
    # لا يمكن الوصول إليها بدون تسجيل دخول
    pass
```

---

## 📱 تدفق المستخدم

### عملية التسجيل الجديد
```
1. المستخدم يذهب إلى /register
2. يملأ النموذج (الاسم، اسم المستخدم، البريد، كلمة المرور)
3. يوافق على الشروط والأحكام
4. يضغط "إنشاء الحساب"
5. يتم التحقق من صحة البيانات
6. يتم حفظ المستخدم في قاعدة البيانات
7. تسجيل دخول تلقائي
8. توجيه إلى الرئيسية
```

### عملية تسجيل الدخول
```
1. المستخدم يذهب إلى /login
2. يدخل اسم المستخدم وكلمة المرور
3. يضغط "دخول"
4. التحقق من صحة بيانات المستخدم
5. التحقق من كلمة المرور
6. إنشاء جلسة جديدة
7. حفظ الجلسة في قاعدة البيانات
8. توجيه إلى الرئيسية
```

### عملية الملف الشخصي
```
1. المستخدم يضغط على اسمه في الشريط العلوي
2. يرى الملف الشخصي
3. يمكنه عرض معلوماته
4. يمكنه ترقية خطته
5. يمكنه تغيير تفضيلاته
6. يمكنه تسجيل الخروج
```

---

## 🚀 كيفية الاستخدام

### تشغيل التطبيق
```bash
cd "d:\GOLD PRO"
python web_app.py
```

### الوصول إلى التطبيق
```
التسجيل الجديد: http://localhost:5000/register
تسجيل الدخول:   http://localhost:5000/login
الملف الشخصي:    http://localhost:5000/profile
```

### API Endpoints
```bash
# الحصول على بيانات المستخدم الحالي
GET /api/user

# تحديث خطة المستخدم
POST /api/subscribe
Content-Type: application/json
{
    "plan": "bronze"
}
```

---

## 💾 قاعدة البيانات

### ملف Database
```
d:\GOLD PRO\users.db
```

### جداول قاعدة البيانات

#### جدول users
```sql
CREATE TABLE users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    username TEXT UNIQUE NOT NULL,
    email TEXT UNIQUE NOT NULL,
    password_hash TEXT NOT NULL,
    full_name TEXT,
    plan TEXT DEFAULT 'free',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    last_login TIMESTAMP,
    is_active BOOLEAN DEFAULT 1
)
```

#### جدول user_sessions
```sql
CREATE TABLE user_sessions (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    session_token TEXT UNIQUE NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    expires_at TIMESTAMP,
    ip_address TEXT
)
```

#### جدول user_activity
```sql
CREATE TABLE user_activity (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    action TEXT,
    timestamp TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    details TEXT
)
```

---

## 🧪 اختبار النظام

### تسجيل مستخدم اختبار
```python
from user_manager import user_manager

result = user_manager.register_user(
    username='testuser',
    email='test@example.com',
    password='password123',
    full_name='Test User'
)
print(result)
# {'success': True, 'message': 'تم التسجيل بنجاح', 'user_id': 1}
```

### تسجيل دخول الاختبار
```python
result = user_manager.login_user('testuser', 'password123')
print(result)
# {'success': True, 'session_token': '...', 'user_id': 1}
```

### التحقق من الجلسة
```python
session_info = user_manager.verify_session(session_token)
print(session_info)
# {'success': True, 'user_id': 1, 'username': 'testuser', ...}
```

---

## 📊 خطط الاشتراك المتاحة

```
Free (مجاني)
├─ إشارات محدودة (5 إشارات يومياً)
├─ لوحة تحكم أساسية
└─ دعم البريد الإلكتروني

Bronze (برونزي)
├─ إشارات يومية (15 إشارة يومياً)
├─ إشعارات فورية
├─ تحليل متقدم
└─ دعم أولوي

Silver (فضي)
├─ إشارات غير محدودة
├─ تحليل فني متقدم
├─ استشارات شخصية
└─ دعم 24/7

Gold (ذهبي)
├─ كل مميزات Silver
├─ خوارزميات ذكية
├─ إدارة محفظة
└─ استثمارات استراتيجية
```

---

## 🔄 التطوير المستقبلي

### الميزات القادمة
- [ ] مصادقة ثنائية (2FA)
- [ ] استرجاع كلمة المرور عبر البريد
- [ ] نسخ احتياطية تلقائية
- [ ] تكامل مع دفع الفواتير
- [ ] لوحة إدارة للمسؤولين
- [ ] تقارير متقدمة
- [ ] WebSocket للتحديثات الفورية

---

## ❓ الأسئلة الشائعة

### س: كيف أغير كلمة المرور؟
ج: في الملف الشخصي، اضغط على "تغيير" بجانب كلمة المرور.

### س: ماذا لو نسيت كلمة المرور؟
ج: اذهب إلى صفحة تسجيل الدخول واضغط "نسيت كلمة المرور".

### س: هل بيانات حسابي آمنة؟
ج: نعم، نستخدم التشفير PBKDF2 وجلسات آمنة.

### س: كم مدة صلاحية الجلسة؟
ج: 30 يوم من آخر نشاط.

### س: هل يمكنني إلغاء الاشتراك؟
ج: نعم، من خلال الملف الشخصي.

---

## 📞 الدعم والمساعدة

للمساعدة أو الإبلاغ عن مشاكل، يرجى التواصل من خلال:
- 📧 البريد الإلكتروني: support@example.com
- 💬 دعم العملاء الحي
- 📖 مركز المساعدة

---

**تم التطوير بنجاح! ✨**
