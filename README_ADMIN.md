# 📖 دليل تفعيل الأدمن وإضافة المستخدمين

## ✨ الخطوات السريعة

### 🚀 الطريقة الأسهل (3 ثواني فقط!)

```
1. انقر نقرة مزدوجة على: ADD_USER.bat
2. أدخل بيانات المستخدم
3. انقر Enter!
```

---

## 📋 الخيارات المتاحة

### ✅ الخيار 1️⃣: إضافة مستخدم سريع
```bash
ADD_USER.bat
```
- يفتح أداة إضافة سريعة وسهلة
- يطلب البيانات الأساسية فقط
- يسمح بتفعيل الأدمن مباشرة

---

### ✅ الخيار 2️⃣: لوحة التحكم الكاملة
```bash
ADMIN_PANEL.bat
```
- قائمة شاملة بـ 9 خيارات
- إضافة / حذف / ترقية مستخدمين
- إدارة الأدمن والاشتراكات
- تصدير التقارير

---

### ✅ الخيار 3️⃣: من خلال الكود مباشرة

#### إضافة مستخدم جديد:
```python
from vip_subscription_system import SubscriptionManager

sm = SubscriptionManager()
success, msg = sm.add_user(
    user_id=123456789,      # معرف تيليجرام
    username="ali_user",     # الاسم المستخدم
    first_name="علي"        # الاسم الأول
)

print(msg)  # تم التسجيل بنجاح!
```

#### تفعيل الأدمن:
```python
from admin_panel import AdminPanel

admin = AdminPanel()
admin.add_admin(123456789)  # تفعيل كأدمن
```

#### ترقية مستخدم:
```python
success, msg = sm.upgrade_user(
    user_id=123456789,
    plan='gold',            # bronze, silver, gold, platinum
    payment_method='admin'
)

print(msg)  # تم الترقية بنجاح!
```

---

## 🎯 الحالات الشائعة

### 📌 حالة 1: إضافة موظف جديد + تفعيله أدمن

```bash
1. افتح ADD_USER.bat
2. أدخل البيانات
3. اختر Yes عند السؤال عن الأدمن
4. تم! ✅
```

---

### 📌 حالة 2: إضافة عميل ليجرب الخدمة

```bash
1. افتح ADD_USER.bat
2. أدخل بيانات العميل
3. اختر No عند السؤال عن الأدمن
4. اختر Yes عند السؤال عن الترقية
5. اختر bronze
6. تم! ✅
```

---

### 📌 حالة 3: ترقية عميل موجود

```bash
1. افتح ADMIN_PANEL.bat
2. اختر [5] ترقية مستخدم
3. أدخل معرف المستخدم
4. اختر الباقة الجديدة
5. تم! ✅
```

---

## 🔑 كيفية الحصول على معرف المستخدم (User ID)

### الطريقة 1️⃣: من البوت نفسه
```
1. افتح تطبيق Telegram
2. ابحث عن البوت
3. أرسل: /start
4. البوت سيخبرك بمعرفك
```

### الطريقة 2️⃣: من تطبيق UserBot Extractor
```
1. استخدم بوت معرّف المستخدم
2. أرسل رسالة
3. البوت يعطيك معرفك
```

---

## 💰 الباقات المتاحة

| الباقة | السعر | المدة | التوصيات/اليوم |
|--------|-------|-------|-----------------|
| Trial | مجاني | 3 أيام | 1 |
| Bronze | $29 | 30 يوم | 3 |
| Silver | $69 | 90 يوم | 5 |
| Gold | $199 | 365 يوم | 7 |
| Platinum | $499 | 365 يوم | 10 |

---

## ✨ ميزات الأدمن

### صلاحيات الأدمن:
- ✅ إضافة / حذف مستخدمين
- ✅ ترقية المستخدمين
- ✅ عرض جميع البيانات
- ✅ ترسيل إعلانات خاصة
- ✅ إدارة الاشتراكات

---

## 📊 عرض البيانات

### الخيار 1: عرض مستخدم واحد
```bash
ADMIN_PANEL.bat → اختر [6]
```

### الخيار 2: عرض جميع المستخدمين
```bash
ADMIN_PANEL.bat → اختر [7]
```

### الخيار 3: تصدير تقرير
```bash
ADMIN_PANEL.bat → اختر [9]
```

---

## 🛠️ الملفات الرئيسية

| الملف | الوصف |
|--------|--------|
| `ADD_USER.bat` | أداة إضافة مستخدم سريعة |
| `ADMIN_PANEL.bat` | لوحة التحكم الكاملة |
| `admin_panel.py` | كود لوحة التحكم |
| `vip_subscription_system.py` | نظام إدارة الاشتراكات |
| `admin_users.json` | قائمة الأدمن |
| `vip_subscriptions.db` | قاعدة البيانات الرئيسية |

---

## ❓ أسئلة شائعة

### س: كيف أحذف مستخدم؟
```
ج: ADMIN_PANEL.bat → اختر [8]
```

### س: كيف أزيل صلاحيات الأدمن من أحد؟
```
ج: ADMIN_PANEL.bat → اختر [3]
```

### س: كيف أعرف كم يوم متبقي للمستخدم؟
```
ج: ADMIN_PANEL.bat → اختر [6] → أدخل معرفه
```

### س: كيف أحدث الاشتراك المنتهي؟
```
ج: ADMIN_PANEL.bat → اختر [5] → اختر باقة جديدة
```

---

## ⚠️ تحذيرات مهمة

### ⚠️ لا تحذف:
- `vip_subscriptions.db` - قاعدة البيانات الرئيسية!
- `admin_users.json` - قائمة الأدمن

### ⚠️ احسب جيداً:
- لا يمكن حذف مستخدم بسهولة
- تأكد قبل الحذف

### ⚠️ حفظ النسخ الاحتياطية:
```
انسخ هذه الملفات بشكل دوري:
- vip_subscriptions.db
- admin_users.json
```

---

## 🔐 الأمان

### كلمات المرور:
- المستخدمون لا يستخدمون كلمات مرور
- المصادقة عن طريق معرف تيليجرام

### الأدمن:
- الأدمن محفوظ في `admin_users.json`
- يمكن تعديل الملف يدويًا

---

## 📞 للمساعدة

اختبر النظام بهذا الأمر:
```bash
ADMIN_PANEL.bat → اختر [4]
```

---

## 📝 ملاحظات آخرة

- جميع التواريخ بصيغة ISO (YYYY-MM-DD)
- جميع المبالغ بالدولار الأمريكي
- الترميز: UTF-8

---

**آخر تحديث:** 2026-01-23  
**الإصدار:** 1.0
