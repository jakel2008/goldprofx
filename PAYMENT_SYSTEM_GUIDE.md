# 📧 نظام الدفع والتفعيل عبر الإيميل

## 🔄 آلية العمل

### 1️⃣ المستخدم يطلب اشتراك
- يسجل الدخول إلى الموقع
- يختار خطة من صفحة الأسعار (Bronze, Silver, Gold, Platinum)
- يضغط على "اشترك الآن"

### 2️⃣ حفظ الطلب وإرسال الإشعار
- ✅ يتم حفظ الطلب في قاعدة البيانات (`subscription_requests`)
- ✅ يتم إرسال إشعار إلى بريد الدعم: **mahmoodalqaise750@gmail.com**
- ✅ يظهر للمستخدم رسالة: "تم استلام طلبك، سيتم التواصل معك بعد التحقق من الدفع"

### 3️⃣ الأدمن يراجع الطلب
- يفتح لوحة الأدمن: http://localhost:5000/admin
- ينتقل إلى تبويب "📨 طلبات الاشتراك"
- يرى جميع الطلبات المعلقة مع التفاصيل:
  - اسم المستخدم
  - البريد الإلكتروني
  - الخطة المطلوبة
  - السعر
  - تاريخ الطلب

### 4️⃣ التحقق من الدفع
- الأدمن يتواصل مع المستخدم للتأكد من الدفع
- أو يتحقق من حساب الدفع (PayPal, Bank, etc.)

### 5️⃣ تفعيل الاشتراك
- الأدمن يضغط على زر "✅ تفعيل"
- يمكن إضافة ملاحظات (اختياري)
- **يتم تلقائياً:**
  - ✅ **إلغاء جميع الاشتراكات النشطة السابقة للمستخدم**
  - ✅ تفعيل الخطة الجديدة فقط (الخطة الأعلى)
  - ✅ إرسال إيميل تأكيد للمستخدم
  - ✅ تحديث حالة الطلب إلى "مفعل"

**ملاحظة مهمة:** المستخدم لا يمكن أن يكون لديه أكثر من اشتراك نشط واحد. عند الترقية، يتم إلغاء الاشتراك القديم تلقائياً وتفعيل الخطة الجديدة.

### 6️⃣ في حالة الرفض
- الأدمن يضغط على زر "❌ رفض"
- يكتب سبب الرفض
- يتم تحديث حالة الطلب إلى "مرفوض"

---

## 📊 حالات الطلبات

| الحالة | الوصف | اللون |
|--------|-------|-------|
| **معلق** (pending) | في انتظار مراجعة الأدمن | 🟡 أصفر |
| **مفعل** (approved) | تم الموافقة والتفعيل | 🟢 أخضر |
| **مرفوض** (rejected) | تم رفض الطلب | 🔴 أحمر |

---

## 💡 معلومات مهمة

### سياسة الاشتراك الواحد:
- ⚠️ **المستخدم يمكن أن يكون لديه اشتراك نشط واحد فقط**
- عند الترقية إلى خطة جديدة، يتم إلغاء الخطة القديمة تلقائياً
- يتم تفعيل الخطة الأعلى/الجديدة فقط
- هذا يضمن عدم وجود تعارض في الاشتراكات

### بيانات الاتصال:
- **بريد الدعم:** mahmoodalqaise750@gmail.com
- يتم إرسال جميع إشعارات الطلبات الجديدة إلى هذا البريد

### الخطط المتاحة:
| الخطة | السعر | المدة | الإشارات/يوم |
|-------|------|------|--------------|
| Bronze | $29 | 30 يوم | 3 |
| Silver | $69 | 90 يوم | 5 |
| Gold | $199 | 365 يوم | 7 |
| Platinum | $499 | 365 يوم | 10 |

### الملفات المعنية:
- **web_app.py** - Routes و APIs
- **email_service.py** - خدمة إرسال الإيميلات
- **users.db** - قاعدة البيانات (جدول subscription_requests)
- **templates/admin.html** - واجهة لوحة الأدمن

---

## ⚙️ تفعيل SMTP (اختياري)

حالياً النظام يحفظ الطلبات فقط. لإرسال الإيميلات تلقائياً:

1. افتح ملف `email_service.py`
2. عدّل الإعدادات:
```python
self.smtp_enabled = True  # غيرها من False إلى True
self.smtp_user = "your-email@gmail.com"
self.smtp_password = "your-app-password"
```

3. احصل على كلمة مرور التطبيق من Gmail:
   - إعدادات Gmail → الأمان
   - التحقق بخطوتين (فعّله)
   - كلمات مرور التطبيقات

---

## 🎯 المزايا

✅ **أمان أعلى** - لا يتم التفعيل التلقائي قبل التحقق
✅ **مرونة** - يمكن رفض الطلبات غير الصحيحة
✅ **تتبع كامل** - جميع الطلبات محفوظة مع التواريخ
✅ **إشعارات فورية** - الأدمن يعلم بالطلبات الجديدة
✅ **تأكيد للمستخدم** - يصله إيميل عند التفعيل

---

## 📞 الدعم

للمساعدة: mahmoodalqaise750@gmail.com

**آخر تحديث:** 24 يناير 2026
