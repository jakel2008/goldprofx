# 🗺️ نظام الملاحة والروابط - GOLD PRO

## 📌 الملاحة الرئيسية

تم تحسين ملف `base.html` بحيث يعرض الملاحة الصحيحة حسب حالة المستخدم.

---

## 🚀 الملاحة للزائرين (غير المسجلين)

```
الرئيسية → يعيد التوجيه إلى /login
الخطط → عرض الخطط المتاحة
تسجيل الدخول → /login
إنشاء حساب → /register
```

**الصفحات المتاحة للزائرين:**
1. `/login` - تسجيل الدخول
2. `/register` - إنشاء حساب جديد
3. `/plans` - عرض الخطط والأسعار

---

## 👤 الملاحة للمستخدمين المسجلين

```
الرئيسية → /index
الإشارات → /signals (التداول الرئيسي)
لوحة التحكم → /dashboard (ملخص الحساب)
التقارير → /reports (الأداء التاريخي)
الأزواج → /pairs-selection (اختيار الأزواج)
الملف الشخصي → /profile (مع قائمة منسدلة)
  ├─ الملف الشخصي → /profile
  ├─ الخطط → /plans
  └─ تسجيل الخروج → /logout
```

**الصفحات المتاحة للمستخدمين:**
1. `/` - الصفحة الرئيسية (تحويل تلقائي للمستخدمين الجدد)
2. `/signals` - عرض الإشارات مع الفلترة
3. `/dashboard` - لوحة التحكم الشخصية
4. `/reports` - التقارير والإحصائيات
5. `/profile` - الملف الشخصي وتعديل البيانات
6. `/pairs-selection` - اختيار الأزواج المفضلة
7. `/plans` - الخطط والاشتراكات
8. `/logout` - تسجيل الخروج

---

## ⚡ الملاحة للمسؤولين (Admin)

```
⚡ الإدارة (قائمة منسدلة)
  ├─ لوحة الأدمن → /admin_panel
  ├─ إدارة المستخدمين → /admin_management
  └─ إدارة الاشتراكات → /subscriptions_management
```

**الصفحات الإدارية:**
1. `/admin_panel` - لوحة التحكم الرئيسية
2. `/admin_management` (أو `/admin/users`) - إدارة المستخدمين
3. `/subscriptions_management` (أو `/admin/subscriptions`) - إدارة الاشتراكات

**حسابات الأدمن المعروفة:**
- `jakel2008` (مسؤول رئيسي)
- أي مستخدم معلن كمسؤول في `admin_users.json`

---

## 🔗 الخرائط الكاملة للروابط

### مسار المستخدم الجديد

```
START
  ↓
/register (إنشاء حساب)
  ↓
اختيار الخطة → /select_location/plan
  ↓
اختيار الدولة → /payment/location/plan
  ↓
إتمام الدفع
  ↓
/login (تسجيل الدخول)
  ↓
/signals (عرض الإشارات)
  ↓
/dashboard (لوحة التحكم)
  ↓
/profile (الملف الشخصي)
```

### مسار المستخدم الحالي

```
/login (تسجيل الدخول)
  ↓
/signals (الإشارات الحالية)
  ↓
/dashboard (الإحصائيات)
  ↓
/reports (التقارير)
  ↓
/pairs-selection (تخصيص الأزواج)
  ↓
/profile (إدارة الحساب)
```

### مسار المسؤول

```
/login (تسجيل دخول الأدمن)
  ↓
/admin_panel (لوحة الأدمن الرئيسية)
  ↓
/admin_management (إدارة المستخدمين)
  ↓
/subscriptions_management (إدارة الاشتراكات)
  ↓
/signals (عرض جميع الإشارات)
```

---

## 📊 جدول الروابط والملفات

| المسار | الملف | الحالة | الوصول |
|-------|------|--------|--------|
| `/` | index.html | ✅ | جميع + تحويل للمستخدمين |
| `/login` | login.html | ✅ | عام |
| `/register` | register.html | ✅ | عام |
| `/logout` | --- | ✅ | مسجلين فقط |
| `/profile` | profile.html | ✅ | مسجلين فقط |
| `/signals` | signals.html | ✅ | مسجلين فقط |
| `/dashboard` | dashboard.html | ✅ | مسجلين فقط |
| `/reports` | reports.html | ✅ | مسجلين فقط |
| `/plans` | plans.html | ✅ | جميع |
| `/pairs-selection` | pairs_selection.html | ✅ | مسجلين فقط |
| `/select_location/<plan>` | select_location.html | ✅ | جميع |
| `/payment/<location>/<plan>` | payment_*.html | ✅ | جميع |
| `/admin_panel` | admin_panel.html | ✅ | أدمن فقط |
| `/admin_management` | admin_management.html | ✅ | أدمن فقط |
| `/subscriptions_management` | subscriptions_management.html | ✅ | أدمن فقط |
| `/api/signals` | --- | ✅ | API |
| `/api/stats` | --- | ✅ | API |
| `/api/user` | --- | ✅ | API |
| `/api/available-pairs` | --- | ✅ | API |

---

## 🔐 حماية الروابط

### طبقة الأمان الأولى: التوجيه
```python
# إذا كان المستخدم غير مسجل
/index → /login (302 Redirect)
/signals → /login (302 Redirect)
```

### طبقة الأمان الثانية: الجلسة
```python
# التحقق من session['user_id'] قبل عرض المحتوى
def login_required(f):
    @wraps(f)
    def decorated_function(*args, **kwargs):
        if not session.get('user_id'):
            return redirect(url_for('login'))
        return f(*args, **kwargs)
    return decorated_function
```

### طبقة الأمان الثالثة: الفلترة
```python
# فلترة الإشارات حسب الخطة
if session.get('is_admin'):
    signals = all_signals  # الأدمن يرى الكل
else:
    signals = filter_by_quality(signals, user_tier)
```

---

## 🎯 مميزات الملاحة الجديدة

### ✨ الميزات المضافة

1. **قوائم منسدلة ديناميكية**
   - قائمة المستخدم (ملف شخصي + خروج)
   - قائمة الأدمن (لوحة تحكم + إدارة)

2. **ملاحة حسب الحالة**
   - نص مختلف للزائرين والمستخدمين
   - أزرار مختلفة حسب الحالة

3. **أيقونات توضيحية**
   - 🏠 الرئيسية
   - 📊 الإشارات
   - 📈 لوحة التحكم
   - 📋 التقارير
   - ⚙️ الأزواج
   - 👤 الملف الشخصي
   - 💰 الخطط
   - ⚡ الإدارة

---

## 🧪 اختبار الملاحة

### اختبار للزائر (غير مسجل)
```
1. افتح http://localhost:5000/
   ✓ يجب أن يحول إلى /login
   
2. انظر للملاحة
   ✓ يجب أن تظهر: الخطط، تسجيل الدخول، إنشاء حساب
   ✗ لا يجب أن تظهر: الإشارات، لوحة التحكم، التقارير
```

### اختبار للمستخدم (مسجل)
```
1. سجل الدخول بحساب عادي
   ✓ يجب أن يظهر في الملاحة:
     - الرئيسية
     - الإشارات
     - لوحة التحكم
     - التقارير
     - الأزواج
     - الملف الشخصي
   ✗ لا يجب أن يظهر: الإدارة
```

### اختبار للمسؤول
```
1. سجل الدخول بحساب jakel2008
   ✓ يجب أن يظهر: جميع الروابط
   ✓ يجب أن يظهر: قائمة الإدارة
   ✓ يجب أن تؤدي /admin_panel للوحة الأدمن
```

---

## 🔄 تحديثات الملاحة

### تاريخ التحديث
- **2026-01-27**: إضافة قوائم منسدلة وملاحة ديناميكية

### التحسينات المستقبلية
- [ ] أيقونات أفضل وأكثر وضوحاً
- [ ] سرعة تحميل أسرع
- [ ] دعم الهاتف المحمول (Responsive)
- [ ] تصميم معاصر (Modern UI)
- [ ] وضع داكن (Dark Mode)

---

## 📱 الملاحة على الهاتف المحمول

```css
@media (max-width: 768px) {
    nav {
        min-width: 100%;
    }
    .nav-links {
        gap: 0.5rem;
        font-size: 0.9rem;
    }
    /* قائمة هامبرجر للهاتف */
}
```

---

## 💡 نصائح الاستخدام

1. **للمستخدمين الجدد**
   - ابدأ من `/register` → `/plans` → `/payment/...`
   - ثم `/login` → `/signals` → `/dashboard`

2. **للمستخدمين الحاليين**
   - روابط سريعة في الملاحة العليا
   - قائمة منسدلة في الملف الشخصي

3. **للمسؤولين**
   - استخدم قائمة الإدارة للتحكم
   - لا تعدل البيانات من `/signals` مباشرة

---

**تم التحديث:** 27 يناير 2026  
**الإصدار:** 2.0  
**الحالة:** جاهز للاستخدام ✅

