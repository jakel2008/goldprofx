# 📖 دليل صلاحيات الأدمن - Gold Analyzer VIP System

## 🔐 تسجيل الدخول كأدمن
- اسم المستخدم: `jakel2008`
- كلمة المرور: `Mahmoud*750`
- رابط لوحة الأدمن: http://localhost:5000/admin

---

## 👥 إدارة المستخدمين

### الوظائف المتاحة:

#### 1. عرض جميع المستخدمين ✅
- عرض قائمة كاملة بجميع المستخدمين المسجلين
- عرض التفاصيل: رقم المستخدم، اسم المستخدم، البريد، الاسم، الخطة، حالة التفعيل، صلاحيات الأدمن، آخر تسجيل دخول

#### 2. ترقية المستخدمين إلى أدمن ⭐
- الضغط على زر "ترقية" لإعطاء صلاحيات أدمن لأي مستخدم
- الضغط على زر "إزالة أدمن" لإزالة صلاحيات الأدمن
- **API Endpoint:** `POST /api/admin/set_admin`
- **البارامترات:**
  ```json
  {
    "user_id": 1,
    "is_admin": true
  }
  ```

#### 3. تفعيل/إيقاف المستخدمين 🔄
- تفعيل أو إيقاف أي مستخدم بضغطة زر واحدة
- المستخدمون الموقوفون لا يستطيعون تسجيل الدخول
- **API Endpoint:** `POST /api/admin/set_active`
- **البارامترات:**
  ```json
  {
    "user_id": 1,
    "is_active": true
  }
  ```

#### 4. تغيير خطة المستخدم 💎
- تغيير خطة أي مستخدم مباشرة من القائمة المنسدلة
- **يتم تلقائياً إلغاء الاشتراكات السابقة وتفعيل الخطة الجديدة فقط**
- الخطط المتاحة:
  - **Free** (مجاني)
  - **Bronze** (برونزي - 29$ / 30 يوم / 3 إشارات/يوم)
  - **Silver** (فضي - 69$ / 90 يوم / 5 إشارات/يوم)
  - **Gold** (ذهبي - 199$ / 365 يوم / 7 إشارات/يوم)
  - **Platinum** (بلاتيني - 499$ / 365 يوم / 10 إشارات/يوم)
- **API Endpoint:** `POST /api/admin/update_plan`
- **البارامترات:**
  ```json
  {
    "user_id": 1,
    "plan": "gold",
    "duration_days": 365  // اختياري
  }
  ```

---

## 💎 إدارة الاشتراكات

### الوظائف المتاحة:

#### 0. طلبات الاشتراك المعلقة 📨 **[جديد]**
- عرض جميع طلبات الاشتراك التي في انتظار التفعيل
- **آلية العمل:**
  1. المستخدم يختار خطة من صفحة الأسعار
  2. يتم حفظ الطلب في قاعدة البيانات
  3. **يتم إرسال إيميل تلقائي إلى:** mahmoodalqaise750@gmail.com
  4. الأدمن يراجع الطلب ويتحقق من الدفع
  5. الأدمن يوافق/يرفض الطلب من لوحة التحكم
  6. **عند الموافقة:** يتم إلغاء الاشتراكات السابقة + تفعيل الخطة الجديدة + إرسال إيميل تأكيد

- **سياسة الاشتراك الواحد:** 
  - ⚠️ المستخدم يمكن أن يكون لديه اشتراك نشط واحد فقط
  - عند التفعيل، يتم إلغاء جميع الاشتراكات النشطة السابقة تلقائياً
  - يتم تفعيل الخطة الجديدة (الأعلى) فقط

- **معلومات الطلب المعروضة:**
  - رقم الطلب
  - اسم المستخدم والبريد الإلكتروني
  - الخطة المطلوبة والسعر
  - المدة بالأيام
  - تاريخ ووقت الطلب
  - حالة الطلب (معلق/مفعل/مرفوض)

- **الإجراءات المتاحة:**
  - ✅ **تفعيل الاشتراك:** تفعيل فوري مع إرسال إيميل تأكيد
  - ❌ **رفض الطلب:** رفض مع إمكانية كتابة سبب الرفض

- **API Endpoint:** `GET /api/admin/subscription_requests`

#### 1. عرض جميع الاشتراكات 📊
- عرض قائمة كاملة بجميع الاشتراكات في النظام
- عرض التفاصيل:
  - رقم المستخدم (Telegram User ID)
  - اسم المستخدم
  - الاسم الأول
  - الخطة الحالية
  - تاريخ بداية الاشتراك
  - تاريخ نهاية الاشتراك
  - حالة الاشتراك (نشط، تجريبي، ملغي، منتهي)
  - إجمالي المبلغ المدفوع

#### 2. تمديد الاشتراكات ⏰
- تمديد اشتراك أي مستخدم بعدد أيام محدد
- يظهر مربع حوار لإدخال عدد الأيام
- يتم إضافة الأيام إلى تاريخ انتهاء الاشتراك الحالي
- **API Endpoint:** `POST /api/admin/extend_subscription`
- **البارامترات:**
  ```json
  {
    "user_id": 123456789,
    "days": 30
  }
  ```

#### 3. إلغاء الاشتراكات ❌
- إلغاء اشتراك أي مستخدم فوراً
- يتم تعيين تاريخ الانتهاء إلى الوقت الحالي
- يتم تغيير حالة الاشتراك إلى "ملغي"
- **API Endpoint:** `POST /api/admin/cancel_subscription`
- **البارامترات:**
  ```json
  {
    "user_id": 123456789
  }
  ```

#### 4. إعادة تفعيل الاشتراكات ✅
- إعادة تفعيل الاشتراكات الملغية أو المنتهية
- يتم إنشاء اشتراك جديد بنفس الخطة السابقة
- يتم حساب مدة الاشتراك بناءً على الخطة
- **API Endpoint:** `POST /api/admin/reactivate_subscription`
- **البارامترات:**
  ```json
  {
    "user_id": 123456789
  }
  ```

---

## 🎨 واجهة لوحة الأدمن

### تبويب المستخدمين 👥
- جدول تفاعلي يعرض جميع المستخدمين
- إمكانية التفعيل/الإيقاف الفوري
- إمكانية الترقية/الحذف من صلاحيات الأدمن
- تغيير الخطة مباشرة من القائمة المنسدلة

### تبويب الاشتراكات 💎
- جدول تفاعلي يعرض جميع الاشتراكات
- رموز ملونة لحالات الاشتراك:
  - 🟢 **نشط** (أخضر)
  - 🔵 **تجريبي** (أزرق)
  - 🔴 **ملغي** (أحمر)
  - 🟡 **منتهي** (أصفر)
- أزرار إجراءات سريعة لكل اشتراك

### تبويب طلبات الاشتراك 📨 **[جديد]**
- عرض جميع الطلبات المعلقة والمعالجة
- معلومات كاملة عن كل طلب
- أزرار الموافقة/الرفض مع ملاحظات
- تحديث تلقائي بعد كل إجراء
- إشعار بعنوان البريد الإلكتروني للدعم

---

## 📡 APIs المتاحة للأدمن

### إدارة المستخدمين:
```
GET  /api/admin/users              - جلب جميع المستخدمين
POST /api/admin/set_admin          - تعيين/إزالة صلاحيات أدمن
POST /api/admin/set_active         - تفعيل/إيقاف مستخدم
POST /api/admin/update_plan        - تحديث خطة مستخدم
```

### إدارة الاشتراكات:
```
GET  /api/admin/subscriptions              - جلب جميع الاشتراكات
POST /api/admin/extend_subscription        - تمديد اشتراك
POST /api/admin/cancel_subscription        - إلغاء اشتراك
POST /api/admin/reactivate_subscription    - إعادة تفعيل اشتراك
GET  /api/admin/subscription_requests      - جلب طلبات الاشتراك [جديد]
POST /api/admin/approve_subscription       - الموافقة على طلب [جديد]
POST /api/admin/reject_subscription        - رفض طلب [جديد]
```

---

## 🔒 الأمان

### حماية مسارات الأدمن:
- جميع مسارات الأدمن محمية بـ `@admin_required` decorator
- يتم التحقق من:
  1. وجود جلسة نشطة (`session_token`)
  2. صلاحية الجلسة
  3. وجود صلاحيات أدمن (`is_admin = True`)
- عند الفشل يتم التحويل تلقائياً إلى الصفحة الرئيسية

### قاعدة البيانات:
- يتم تشفير كلمات المرور باستخدام PBKDF2-HMAC-SHA256
- الجلسات تنتهي بعد 30 يوم
- يتم تسجيل جميع الأنشطة في جدول `user_activity`

---

## 📝 ملاحظات مهمة

1. **نظام الدفع والتفعيل الجديد:**
   - عند طلب اشتراك جديد، يتم حفظ الطلب في قاعدة البيانات
   - يتم إرسال إشعار تلقائي إلى: **mahmoodalqaise750@gmail.com**
   - الأدمن يراجع الطلب ويتحقق من استلام الدفع
   - الأدمن يوافق على الطلب من تبويب "طلبات الاشتراك"
   - **عند الموافقة:** يتم تفعيل الاشتراك تلقائياً وإرسال إيميل تأكيد للمستخدم

2. **التكامل بين الأنظمة:**
   - يوجد نظامان للمستخدمين:
     - `users.db` - للمستخدمين على الويب
     - `vip_subscriptions.db` - للمشتركين عبر التليجرام
   - عند تغيير الخطة يتم التحديث في كلا النظامين

3. **الخطط والصلاحيات:**
   - كل خطة لها عدد محدد من الإشارات اليومية
   - يمكن تخصيص مدة الاشتراك عند التحديث

4. **النسخ الاحتياطي:**
   - يُنصح بعمل نسخة احتياطية من قواعد البيانات بانتظام
   - الملفات: `users.db` و `vip_subscriptions.db`

5. **المراقبة:**
   - جميع التغييرات يتم تسجيلها في جدول `user_activity`
   - يمكن متابعة نشاط المستخدمين من خلال السجلات

6. **إعدادات SMTP (اختياري):**
   - لإرسال الإيميلات تلقائياً، يمكن تفعيل SMTP في `email_service.py`
   - قم بإضافة بريد Gmail وكلمة مرور التطبيق
   - حالياً: يتم حفظ الطلبات فقط دون إرسال إيميل فعلي

---

## 🚀 البدء السريع

1. **تشغيل النظام:**
   ```bash
   cd "D:\GOLD PRO"
   python web_app.py
   ```

2. **الوصول إلى لوحة الأدمن:**
   - افتح المتصفح على: http://localhost:5000/admin
   - سجل الدخول بحساب الأدمن

3. **إدارة المستخدمين:**
   - تبويب "المستخدمون" - لإدارة حسابات المستخدمين
   - تبويب "الاشتراكات" - لإدارة اشتراكات VIP

---

## ❓ الأسئلة الشائعة

**س: كيف يعمل نظام الدفع الجديد؟**
ج: عندما يطلب مستخدم اشتراك، يتم حفظ الطلب وإرسال إيميل إلى mahmoodalqaise750@gmail.com. بعد التحقق من الدفع، يقوم الأدمن بتفعيل الاشتراك من تبويب "طلبات الاشتراك".

**س: كيف أضيف أدمن جديد؟**
ج: استخدم `setup_admin.py` لإنشاء أدمن جديد، أو قم بترقية مستخدم موجود من لوحة الأدمن.

**س: هل يمكن استعادة اشتراك ملغي؟**
ج: نعم، استخدم زر "إعادة تفعيل" في تبويب الاشتراكات.

**س: كيف أغير مدة اشتراك معين؟**
ج: استخدم زر "تمديد" وأدخل عدد الأيام المطلوبة.

**س: هل يمكن تعيين أكثر من أدمن؟**
ج: نعم، يمكن ترقية أي عدد من المستخدمين إلى أدمن.

**س: كيف أفعل إرسال الإيميلات تلقائياً؟**
ج: قم بتعديل ملف `email_service.py` وأضف بيانات SMTP (بريد Gmail وكلمة مرور التطبيق)، ثم غيّر `smtp_enabled = True`.

---

## 📞 الدعم الفني

للمساعدة والدعم:
- التواصل عبر التليجرام: @ABOOHASHEMFXBOT
- البريد الإلكتروني: support@goldanalyzer.com

---

**آخر تحديث:** 24 يناير 2026
**الإصدار:** 2.0
