## ملاحظات GOLD PRO للمساعدات الذكية

### الصورة الكاملة / العمارة البرمجية
- **مستودع Python أحادي** لمنصة إشارات التداول: واجهة Flask، بث خلفي، محركات تحليل، إدارة اشتراكات، والترخيص.
- **ثلاث قواعد بيانات**: `goldpro_system.db` (مستخدمون، خطط، صفقات)، `vip_subscriptions.db` (حسابات الاشتراك)، `activations.db` (مفاتيح الترخيص).
- **تدفق الإشارات**: ملفات JSON في `signals/` → `signal_broadcaster.py` يزيل التكرار وينسق → Telegram عبر قوالب `signal_formatter.py` → تتبع الصفقات في `trades_tracker.json`.
- **خط أنابيب التحليل**: `analysis_engine.py` (8+ استراتيجيات: ICT/SMC، RSI/MACD، EMA، Bollinger، Stochastic، Fibonacci، Ichimoku، Pivots) و `recommendations_engine.py` يجلبان بيانات `yfinance` → إخراج JSON إلى `analysis/` و `recommendations/`.
- **طبقات الاشتراك**: مجاني، برونز، فضي، ذهبي، بلاتيني (معرّفة في قاموس `vip_subscription_system.py::PLANS`) مع حدود المدة والإشارات.
- **السبب وراء هذه العمارة**: فصل واجهة الويب والبث والتحليل يسمح بالقياس المستقل. الإشارات المستندة إلى الملفات تمكّن التكامل السهل مع الأنظمة الخارجية.

### اتفاقيات البيانات والملفات
- **الإشارات**: JSON في `signals/` مع أسماء حقول موحدة: `symbol`/`pair`، `signal_type`/`trade_type`/`signal`، `entry_price`/`entry`، `stop_loss`/`sl`، `take_profit_1..3`/`tp1..3`. مثال: `{"symbol": "EURUSD", "signal_type": "buy", "entry": 1.0850, "sl": 1.0820, "tp1": 1.0900}`.
- **إزالة التكرار**: الإشارات تُتبع في `sent_signals.json` (قائمة بالكائنات تحتوي `signal_id`، `signature`، `sent_at`). الباث يفحص قبل الإرسال لتجنب التكرار.
- **بيانات السوق**: تستخدم خريطة `yfinance` (مثلاً EURUSD → 'EURUSD=X'، XAUUSD → 'GC=F'، BTC → 'BTC-USD'). انظر `recommendations_engine.py::ALL_AVAILABLE_PAIRS` للخريطة الكاملة.
- **تنسيق Telegram**: `signal_formatter.py` ينتج Markdown بدقة 5 عشريات وأيقونات بيانات (🟢 شراء، 🔴 بيع). الطوابع الزمنية بصيغة 'YYYY-MM-DD HH:MM'.

### الأنماط الحرجة المراد اتباعها
- **توحيد حقول الإشارة**: دعم الأسماء البديلة دائماً عند قراءة الإشارات. انظر `signal_broadcaster.py` و `web_app.py` لنمط `normalize_signal()`.
- **هجرات SQLite**: استخدم الحراسات (`ALTER TABLE ... IF EXISTS`، فحوصات `IF NOT EXISTS`) في ملفات المخطط وإدارة الاشتراكات. لا توجد ORM معقدة — SQL مباشر.
- **متغيرات البيئة**: بيانات Telegram عبر `MM_TELEGRAM_BOT_TOKEN` و `MM_TELEGRAM_CHAT_ID`. القيم الافتراضية في الكود (غير الإنتاج)؛ تجاوز عبر env.
- **الترميز على Windows**: CI/CD على Windows يتطلب `os.system('chcp 65001 > nul')` لـ UTF-8. انظر `admin_panel.py`.
- **مسارات الملفات**: استخدم `pathlib.Path()` (مثلاً `Path(__file__).parent / "signals"`). يتجنب مشاكل Windows/Unix.

### سير عمل المطورين
- **واجهة الويب**: شغّل `python web_app.py` على المنفذ 5000. مهمة VS Code `restart-web-server` (PowerShell) توقف أي عملية على :5000 وتعيد التشغيل. للتكرار السريع، عدّل `../templates/` HTML مباشرة.
- **باث الإشارات**: عملية طويلة الأجل. استخدم مهمة VS Code `broadcast-signals` أو `python signal_broadcaster.py`. يستطلع `signals/` كل 5-10 ثوان (تحقق من الحلقة في الكود).
- **التحليل**: شغّل `python analysis_engine.py` تفاعلياً أو استدعِ `AnalysisEngine().analyze_all()` لإنشاء JSON في `analysis/` والتوصيات.
- **عمليات المسؤول**: `python admin_panel.py` لإدارة المستخدمين عبر CLI (إضافة/ترقية/منح مسؤول). يحدّث `vip_subscriptions.db` و `admin_users.json`.

### نقاط التكامل والتبعيات
- **yfinance + ta (تحليل تقني)**: بيانات السوق والمؤشرات. مستخدمة في التحليل والتوصيات وتتبع الصفقات. تعامل مع أخطاء المهلة الزمنية والشبكة بحنان (فحوصات None).
- **Telegram Bot API**: HTTP POST إلى `https://api.telegram.org/bot{TOKEN}/sendMessage`. يعيد الباث محاولة على 429 (حد المعدل) أو أخطاء الشبكة.
- **جلسة SQLite**: استخدم مدير السياق `get_db()` في مسارات Flask. أغلق دائماً `conn.close()` لتجنب انتظار الأقفال. انظر `web_app.py` لمثال على إعداد قاعدة البيانات.
- **جلسة Flask**: حياة جلسة دائمة لمدة 7 أيام. المصادقة عبر email/كلمة مرور hash. انظر `web_app.py` لإعداد مفتاح السر.

### نصائح الاختبار والتصحيح
- تحقق من `sent_signals.json` لتشخيص "لم يتم إرسال الإشارة" — إذا كانت المدخلة موجودة، الباث معالجها.
- تحقق من متغيرات Telegram env مع `echo $env:MM_TELEGRAM_BOT_TOKEN` (PowerShell) أو `env | grep MM_`.
- استخدم `track_trades.py::get_current_price()` للتحقق من اتصال yfinance للرمز.
- المسؤولون مخزنون في `admin_users.json` (مجموعة JSON). المستخدمون في `vip_subscriptions.db`. زامن يدويّاً إذا لزم الأمر.
