# 🚀 البدء السريع - نظام التحليل والتوصيات
**Quick Start Guide - Analysis & Recommendations System**

---

## ⚡ تشغيل سريع (30 ثانية)

### 1. افتح القاذف التفاعلي:
```batch
START_ANALYSIS_SYSTEM.bat
```

### 2. اختر من القائمة:
- **[4]** لإعداد التفضيلات (مرة واحدة فقط)
- **[3]** لتوليد التوصيات
- **[1]** لتحليل زوج محدد

---

## 📦 المتطلبات

```bash
# المكتبات المطلوبة (مثبتة بالفعل):
yfinance    # بيانات السوق
pandas      # معالجة البيانات
ta          # المؤشرات الفنية
numpy       # الحسابات الرياضية
```

---

## 🎯 الأوامر الرئيسية

### توليد التوصيات:
```python
python recommendations_engine.py
```

### تحليل تفاعلي:
```python
python analysis_engine.py
```

### اختبار النظام:
```python
python test_recommendations.py
python test_analysis.py
```

---

## 📂 الملفات المهمة

| الملف | الوصف |
|-------|-------|
| `user_preferences.json` | تفضيلاتك (فئات، أزواج، عتبة الجودة) |
| `recommendations/` | التوصيات المولدة |
| `analysis/` | نتائج التحليل |
| `ANALYSIS_SYSTEM_GUIDE.md` | الدليل الكامل |
| `TEST_REPORT.md` | تقرير الاختبار |

---

## 🔧 الإعدادات السريعة

### تعديل التفضيلات يدوياً:
```json
{
  "categories": ["forex_major", "metals", "crypto"],
  "timeframes": ["1h", "4h", "1d"],
  "min_quality_score": 70,
  "risk_per_trade": 2.0
}
```

### الفئات المتاحة:
- `forex_major` (7 أزواج)
- `forex_minor` (8 أزواج)
- `metals` (4 معادن)
- `energy` (5 منتجات)
- `indices_us` (5 مؤشرات)
- `indices_europe` (5 مؤشرات)
- `indices_asia` (5 مؤشرات)
- `crypto` (6 عملات)

---

## 📊 أمثلة الاستخدام

### مثال 1: تحليل EUR/USD
```python
from analysis_engine import AnalysisEngine

engine = AnalysisEngine()
result = engine.analyze_symbol("EURUSD", "EURUSD=X", "1d")
print(f"الإجماع: {result['consensus']['signal']}")
```

### مثال 2: توليد التوصيات
```python
from recommendations_engine import RecommendationsEngine

engine = RecommendationsEngine()
recommendations = engine.scan_all_pairs()
print(f"عدد التوصيات: {len(recommendations)}")
```

---

## ⚠️ استكشاف الأخطاء

### الخطأ: ModuleNotFoundError
```bash
# الحل:
pip install yfinance pandas ta numpy
```

### الخطأ: لا توجد بيانات
```
# السبب: السوق مغلق أو الرمز خاطئ
# الحل: استخدم رموز Yahoo Finance الصحيحة
# مثال: EURUSD=X ليس EURUSD
```

### الخطأ: لا توجد توصيات
```
# السبب: لا توجد فرص تتجاوز عتبة الجودة (70)
# الحل: خفض min_quality_score في التفضيلات
```

---

## 📞 الدعم

راجع الدليل الكامل: `ANALYSIS_SYSTEM_GUIDE.md`  
تقرير الاختبار: `TEST_REPORT.md`

---

**الحالة:** ✅ جاهز للاستخدام  
**الإصدار:** 1.0.0  
**آخر تحديث:** 24 يناير 2026
