@echo off
chcp 65001 > nul
title المحلل المتقدم الكامل - MONEY MAKER
color 0A

echo.
echo ============================================================
echo          المحلل المتقدم الكامل - MONEY MAKER VIP
echo ============================================================
echo.
echo 🚀 جاري بدء التشغيل...
echo.

REM تفعيل البيئة الافتراضية
if exist ".venv-1\Scripts\activate.bat" (
    call .venv-1\Scripts\activate.bat
) else if exist "venv\Scripts\activate.bat" (
    call venv\Scripts\activate.bat
) else if exist ".venv\Scripts\activate.bat" (
    call .venv\Scripts\activate.bat
) else (
    echo ❌ لم يتم العثور على البيئة الافتراضية
    pause
    exit /b 1
)

REM تشغيل الاختبار
echo 📋 تشغيل الاختبار الشامل...
echo.
python test_advanced_complete.py
echo.

if %ERRORLEVEL% EQU 0 (
    echo.
    echo ============================================================
    echo ✅ النظام جاهز للاستخدام!
    echo ============================================================
    echo.
    echo 📝 تعليمات الاستخدام:
    echo.
    echo 1. افتح المتصفح على: http://localhost:5000
    echo 2. سجل الدخول بحساب الأدمن
    echo 3. اضغط على زر "المحلل المتقدم" (الزر الأرجواني)
    echo 4. اختر الزوج والإطار الزمني
    echo 5. اضغط على "تحليل"
    echo 6. انتظر النتائج
    echo 7. يمكنك:
    echo    - نشر على التوصيات
    echo    - إرسال للبوت
    echo    - تصدير JSON
    echo    - مشاركة واتساب/تليجرام
    echo.
    echo ============================================================
    echo.
    
    REM فتح المتصفح تلقائياً
    echo 🌐 فتح المتصفح...
    timeout /t 2 > nul
    start http://localhost:5000
    
    echo.
    echo ✅ تم فتح المتصفح - تأكد من تشغيل web_app.py
    echo.
) else (
    echo.
    echo ❌ فشل الاختبار - راجع الأخطاء أعلاه
    echo.
)

pause
