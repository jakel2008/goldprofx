# متطلبات البدء بالتداول الآلي من الموقع

## 📋 نظرة عامة
نظام التداول الآلي على الموقع يتضمن 3 مكونات رئيسية:
1. **محرك التحليل** - يوليد إشارات التداول
2. **نظام المخاطر** - يحمي رأس المال من الخسائر
3. **محرك التنفيذ** - ينفذ الصفقات على MT5

---

## 🔐 1. متطلبات الأذونات والمصادقة

### أ. صلاحية Admin (إلزامي)
- **الدور المطلوب:** Admin أو Developer
- **الحالة الحالية:** Account `jakel2008` (mahmoodalqaise750@gmail.com) = Developer (صلاحيات كاملة)
- **كيفية التحقق:** 
  - دخول الموقع على http://127.0.0.1:5000/
  - الذهاب إلى لوحة التحكم (Admin Panel)
  - التحقق من Role = "admin" أو "developer"

### ب. الحد الأدنى من الأذونات الأخرى:
- ✅ الوصول إلى الإشارات (Signals)
- ✅ الوصول إلى الصفقات (Trades)
- ✅ الوصول إلى لوحة التحكم (Admin Controls)

---

## 💰 2. متطلبات الحساب والرصيد

### الحد الأدنى للرصيد:
| المتطلب | القيمة الحالية | الحالة |
|---------|----------------|--------|
| **رصيد الحساب** | $546.69 USD | ✅ كافي |
| **الحد الأدنى المقترح** | $500 USD | ✅ أعلى من الحد الأدنى |
| **Margin Level** | 0% (لا توجد صفقات مفتوحة) | ✅ جيد جداً |
| **التداول مفعّل؟** | نعم (True) | ✅ نعم |

### معلومات الحساب الحالي:
```
رقم الحساب: 252471964
الخادم: Exness-MT5Real33
العملة: USD
التداول المسموح: ✅ نعم
```

---

## ⚙️ 3. متطلبات الإعدادات (Configuration)

### أ. إعدادات المخاطر اليومية
```json
{
  "max_risk_usd_per_trade": 12.0,      // أقصى خسارة في الصفقة الواحدة
  "max_risk_percent_of_equity": 1.5,   // أقصى نسبة من رأس المال
  "max_total_open_risk_percent": 1.5,  // أقصى خطر مفتوح 
  "daily_loss_limit_usd": 50.0,        // أقصى خسارة يومية
  "max_consecutive_losses": 5,         // أقصى عدد خسائر متتالية
  "max_open_positions": 3              // أقصى عدد صفقات مفتوحة
}
```

### ب. إعدادات التصفية والثقة
```json
{
  "min_score_gap": 18,         // الحد الأدنى لقوة الإشارة
  "min_rr_ratio": 0.8,         // أقل نسبة Risk/Reward
  "cooldown_minutes_per_symbol": 30  // فترة الانتظار بين الصفقات
}
```

### ج. إعدادات الأوقات
```json
{
  "force_gold_trading_now": true,  // تفعيل التداول بدون تقييد أوقات السوق
  "daily_loss_reset_at_utc": "2026-06-22T03:24:04+00:00"  // وقت إعادة تعيين الخسائر اليومية
}
```

### د. الرموز المدعومة حالياً:
```
XAUUSD (الذهب)     - مفعّل ✅
US30                - مفعّل ✅
EURUSD              - مفعّل ✅
GBPUSD              - مفعّل ✅
BTCUSD              - مفعّل ✅
USDJPY              - مفعّل ✅
```

---

## 🎯 4. متطلبات الإشارات

### أ. معايير الإشارة المقبولة:
```
✅ الإشارات العادية (Normal Signals)
✅ الإشارات القوية (Strong Signals)
✅ أوامر معلقة (Pending Orders)
✅ أوامر السوق (Market Orders)
```

### ب. الفترات الزمنية المدعومة:
```
5m, 15m, 30m (فترات قصيرة)
1h, 4h      (فترات متوسطة)
```

### ج. معايير التصفية:
- ✅ قوة الإشارة (Score Gap) ≥ 18
- ✅ نسبة Risk/Reward ≥ 0.8
- ✅ فترة تبريد 30 دقيقة بين الصفقات من نفس الرمز

---

## 🚀 5. متطلبات البدء الفعلي

### الخطوة 1️⃣: تسجيل الدخول للموقع
```
URL: http://127.0.0.1:5000/
Username: jakel2008
Email: mahmoodalqaise750@gmail.com
Role: Admin/Developer
Status: ✅ جاهز
```

### الخطوة 2️⃣: التحقق من حالة MT5
```
✅ اتصال MT5: متصل
✅ رقم الحساب: 252471964
✅ التداول مفعّل: نعم
✅ الرصيد: $546.69 USD
```

### الخطوة 3️⃣: تفعيل المحرك الآلي
من الموقع:
1. ادخل لوحة التحكم (Admin Panel)
2. ابحث عن "Auto Trader Settings" أو "Automated Trading"
3. اضغط على "Start Auto Trader" / "تشغيل التداول الآلي"

**من Terminal مباشرة:**
```powershell
# إعادة تشغيل محرك التداول الآلي
cd d:\GOLD PRO
& "venv\Scripts\python.exe" continuous_auto_trader.py
```

### الخطوة 4️⃣: مراقبة الصفقات
```
✅ مراقبة الإشارات الجديدة: /signals
✅ مراقبة الصفقات المفتوحة: /trades
✅ مراقبة السجلات: auto_trader_live.out.log
```

---

## ⚠️ 6. نقاط حماية مهمة

### أ. الحدود الحالية المفعّلة:
```
✅ حد الخسارة اليومية: $50 USD
✅ حد الخسائر المتتالية: 5
✅ أقصى مخاطر بالصفقة: $12 USD
✅ أقصى صفقات مفتوحة: 3
```

### ب. آليات الحماية:
- ✅ إعادة تعيين الخسائر اليومية تلقائياً في وقت محدد
- ✅ إيقاف التداول عند تجاوز الحد الأقصى للخسائر
- ✅ فترات انتظار بين الصفقات
- ✅ نسب Risk/Reward دنيا

### ج. الحالة الحالية للمخاطر:
```json
{
  "daily_realized_pnl": -4.15,        // الخسارة اليومية الحالية
  "consecutive_losses": 6,            // عدد الخسائر المتتالية
  "max_allowed_consecutive": 5,       // الحد الأقصى المسموح
  "status": "⚠️ محظور حالياً"          // السبب: exceeded consecutive losses
}
```

**ملاحظة:** التداول محظور حالياً لأن الخسائر المتتالية (6) تتجاوز الحد (5)
- **الحل:** سيتم الفتح تلقائياً عند إعادة تعيين الخسائر أو في اليوم التالي

---

## 🛠️ 7. خطوات استكشاف الأخطاء

### إذا لم يبدأ التداول:

#### 1. تحقق من اتصال MT5:
```powershell
& "venv\Scripts\python.exe" -c "
from mt5_bridge import MT5Bridge
b = MT5Bridge()
status = b.connect()
print(status)
"
```

#### 2. تحقق من رصيد الحساب:
```powershell
& "venv\Scripts\python.exe" -c "
from mt5_bridge import mt5
info = mt5.account_info()
print(f'Equity: {info.equity}')
print(f'Trade Allowed: {info.trade_allowed}')
"
```

#### 3. فحص السجلات:
```
auto_trader_live.out.log          # سجل التداول الآلي
auto_trading_runtime_state.json   # حالة التداول الحالية
auto_trading_user_config.json     # إعدادات المستخدم
```

---

## 📊 8. الحد الأدنى من المتطلبات (Checklist)

تأكد من ✅ كل واحد قبل البدء:

```
☑️  لديك صلاحية Admin على الموقع
☑️  حسابك مسجّل ومفعّل في المتصفح
☑️  اتصال MT5 يعمل (Account 252471964)
☑️  الرصيد ≥ $500 USD
☑️  التداول مفعّل على حسابك في MT5
☑️  ملف auto_trading_user_config.json موجود
☑️  ملف mt5_wallet_config.json موجود (لديك الأسرار)
☑️  خادم الويب يعمل على http://127.0.0.1:5000/
☑️  محرك التحليل يوليد إشارات (تحقق من signals/ folder)
☑️  لا توجد ضوابط أمان تحظر التداول (راجع الحدود)
```

---

## 🎮 9. واجهة الموقع للتحكم بالتداول

### الصفحات الرئيسية:

| الصفحة | الرابط | الوصف |
|-------|--------|--------|
| **لوحة التحكم** | `/` | نظرة عامة على النظام |
| **الإشارات** | `/signals` | قائمة الإشارات الجديدة |
| **الصفقات** | `/trades` | الصفقات المفتوحة والمغلقة |
| **التقارير** | `/api/periodic-report` | تقارير الأداء |
| **الإعدادات** | `/admin` | إدارة الإعدادات |
| **الصحة** | `/healthz` | حالة النظام |

---

## 💡 10. نصائح مهمة

1. **ابدأ صغيراً:** حتى لو كان لديك رصيد كبير، ابدأ بإعدادات محافظة
2. **راقب الأداء:** فحص السجلات بانتظام للتأكد من عمل النظام
3. **تجنب التعارضات:** لا تشغّل محركين آليين بنفس الوقت
4. **احتفظ بالمخزون الاحتياطي:** لا تخاطر برصيدك كاملاً
5. **اعرف الحدود:** تعرّف على إعدادات المخاطر والحماية

---

## 📞 للمساعدة أو الأسئلة:

- فحص صحة النظام: `http://127.0.0.1:5000/healthz`
- عرض السجلات: `auto_trader_live.out.log`
- التحقق من الإشارات: `signals/` folder
- مسح الحالة: `auto_trading_runtime_state.json`

---

**آخر تحديث:** 2026-06-22  
**الحالة:** ✅ النظام جاهز للتداول (مع تجاوز الحد الأقصى للخسائر المتتالية - متوقف مؤقتاً)
