# نظام إدارة الصفقات الذكي

## 📋 نظرة عامة

نظام ذكي لإدارة الصفقات يراعي أوقات فتح وإغلاق الأسواق المختلفة (فوركس، أسهم، سلع، عملات رقمية) ويقوم بتعليق أو إعادة تنشيط الصفقات تلقائياً.

---

## 🎯 المميزات الرئيسية

### 1. إدارة أوقات الأسواق
- **الفوركس**: الأحد 22:00 GMT - الجمعة 22:00 GMT (24/5)
- **المؤشرات الأمريكية**: الإثنين-الجمعة 09:30-16:00 EST
- **السلع** (ذهب، فضة، نفط): مشابهة للفوركس (24/5)
- **العملات الرقمية**: مفتوحة 24/7 بدون توقف

### 2. التعليق التلقائي
- تعليق الصفقات عند إغلاق الأسواق (نهاية الأسبوع)
- نقل الصفقات من `active_trades.json` إلى `suspended_trades.json`
- العملات الرقمية لا تُعلّق أبداً (24/7)

### 3. إعادة التنشيط التلقائية
- إعادة تنشيط الصفقات المعلقة عند افتتاح الأسواق
- فحص دوري كل 4 ساعات
- فحص خاص عند افتتاح الأسبوع (الأحد 22:00 GMT)

### 4. الصيانة اليومية
- تُنفذ كل يوم الساعة 00:05
- تعليق صفقات الأسواق المغلقة
- إعادة تنشيط صفقات الأسواق المفتوحة

### 5. إعادة التعيين الأسبوعية
- تُنفذ كل أحد الساعة 22:05 GMT
- إعادة تنشيط جميع صفقات الفوركس والسلع
- حذف الصفقات القديمة (أكثر من 7 أيام)
- تنظيف ملفات الإشارات المنتهية

---

## 📁 الملفات الأساسية

### 1. `market_hours.py`
نظام إدارة أوقات الأسواق

**الوظائف الرئيسية:**
```python
market_hours = MarketHours()

# فحص إذا كان سوق الفوركس مفتوح
is_open = market_hours.is_forex_open()

# فحص سوق الأسهم الأمريكية
is_open = market_hours.is_us_stock_market_open()

# العملات الرقمية (دائماً مفتوحة)
is_open = market_hours.is_crypto_market_open()

# الحصول على حالة السوق لرمز معين
status = market_hours.get_market_status('EURUSD')
# Returns: {'is_open': True/False, 'market_type': 'forex', 'next_event': '...', 'can_trade': True/False}
```

### 2. `smart_trade_manager.py`
مدير الصفقات الذكي

**الوظائف الرئيسية:**
```python
manager = SmartTradeManager()

# تعليق الصفقات للأسواق المغلقة
stats = manager.suspend_closed_market_trades()

# إعادة تنشيط الصفقات عند افتتاح الأسواق
stats = manager.reactivate_market_open_trades()

# تنظيف الإشارات القديمة
deleted_count = manager.cleanup_old_signals(max_age_days=7)

# صيانة يومية شاملة
report = manager.daily_maintenance()

# إعادة تعيين أسبوعية
report = manager.weekly_reset()

# الحصول على تقرير شامل
report = manager.get_trade_status_report()
```

### 3. `trade_scheduler.py`
جدولة الصيانة التلقائية

**الجدول الزمني:**
- صيانة يومية: 00:05
- إعادة تعيين أسبوعية: الأحد 22:05 GMT
- فحص افتتاح الفوركس: الأحد 22:10 GMT
- فحص إغلاق الفوركس: الجمعة 22:00 GMT
- فحص افتتاح الأسهم: الإثنين-الجمعة 09:35 EST
- فحص إغلاق الأسهم: الإثنين-الجمعة 16:05 EST
- فحص دوري: كل 4 ساعات

### 4. ملفات البيانات

#### `active_trades.json`
قائمة بمعرفات الصفقات النشطة
```json
[
  "EURUSD_20260126_120000",
  "BTCUSD_20260126_120005",
  "XAUUSD_20260126_120010"
]
```

#### `suspended_trades.json`
الصفقات المعلقة مع معلومات إضافية
```json
{
  "SPX_20260124_140000": {
    "symbol": "SPX",
    "suspended_at": "2026-01-26T00:05:00",
    "reason": "السوق مغلق - يفتح الإثنين 09:30 EST",
    "original_timestamp": "20260124_140000"
  }
}
```

---

## 🚀 طريقة الاستخدام

### التشغيل التلقائي الكامل
```batch
AUTO_SIGNAL_SYSTEM.bat
```
يشمل:
- المحلل العميق (كل 5 دقائق)
- نظام البث التلقائي
- بوت التليجرام VIP
- **جدولة الصيانة الذكية** ✨

### تشغيل الجدولة فقط
```batch
START_TRADE_SCHEDULER.bat
```

### اختبار النظام
```batch
TEST_SMART_SYSTEM.bat
```

### أوامر يدوية

#### فحص أوقات الأسواق
```bash
python market_hours.py
```

#### عرض تقرير الصفقات
```bash
python smart_trade_manager.py
```

#### تنفيذ صيانة يدوية
```bash
python -c "from smart_trade_manager import SmartTradeManager; SmartTradeManager().daily_maintenance()"
```

#### إعادة تعيين أسبوعية يدوياً
```bash
python -c "from smart_trade_manager import SmartTradeManager; SmartTradeManager().weekly_reset()"
```

---

## 🔧 المتطلبات

```bash
pip install pytz schedule
```

أو استخدم:
```bash
pip install -r requirements.txt
```

---

## 📊 آلية العمل

```
1. المحلل يولّد إشارات كل 5 دقائق
   ↓
2. الإشارات تُحفظ في signals/ وتُضاف لـ active_trades.json
   ↓
3. المجدول يفحص كل صفقة بشكل دوري:
   ├── السوق مفتوح → تبقى في active_trades.json
   ├── السوق مغلق → تُنقل إلى suspended_trades.json
   └── قديمة (+7 أيام) → تُحذف نهائياً
   ↓
4. عند افتتاح السوق:
   ├── الصفقات المعلقة تعود لـ active_trades.json
   └── البث يرسلها للمشتركين
```

---

## 🎯 الفوائد

✅ **عدم إرسال إشارات للأسواق المغلقة**
- لا توصيات SPX يوم السبت
- لا توصيات EURUSD خارج أوقات الفوركس

✅ **تجديد تلقائي للتوصيات الأسبوعية**
- إعادة تنشيط كل أحد عند افتتاح الأسبوع
- تنظيف التوصيات القديمة

✅ **إدارة ذكية للذاكرة**
- حذف الصفقات القديمة تلقائياً
- تنظيف ملفات الإشارات

✅ **تنظيم أفضل للصفقات**
- فصل واضح بين النشطة والمعلقة
- تتبع سبب التعليق

✅ **تقليل الإرباك للمشتركين**
- إشارات فقط للأسواق النشطة
- توصيات محدثة وذات صلة

---

## 📈 حالات الاستخدام

### السيناريو 1: نهاية الأسبوع (السبت)
```
الجمعة 22:00 GMT:
├── تعليق جميع صفقات الفوركس والسلع
├── تعليق جميع صفقات المؤشرات الأمريكية
└── العملات الرقمية تبقى نشطة (24/7)

Result:
├── active_trades.json → BTCUSD, ETHUSD, etc.
└── suspended_trades.json → EURUSD, SPX, XAUUSD, etc.
```

### السيناريو 2: افتتاح الأسبوع (الأحد 22:00 GMT)
```
الأحد 22:05 GMT:
├── إعادة تنشيط جميع صفقات الفوركس
├── إعادة تنشيط جميع صفقات السلع
├── المؤشرات الأمريكية تبقى معلقة (تفتح الإثنين)
└── حذف الصفقات القديمة (+7 أيام)

Result:
├── active_trades.json → EURUSD, XAUUSD, BTCUSD, etc.
└── suspended_trades.json → SPX, DJI (حتى الإثنين 09:30)
```

### السيناريو 3: افتتاح سوق الأسهم (الإثنين 09:30 EST)
```
الإثنين 09:35 EST:
├── إعادة تنشيط صفقات المؤشرات الأمريكية
└── جميع الأسواق الآن نشطة

Result:
├── active_trades.json → جميع الصفقات
└── suspended_trades.json → فارغ
```

---

## 🔍 المراقبة والصيانة

### التحقق من الحالة الحالية
```python
from smart_trade_manager import SmartTradeManager

manager = SmartTradeManager()
report = manager.get_trade_status_report()

print(f"صفقات نشطة: {report['total_active']}")
print(f"صفقات معلقة: {report['total_suspended']}")
print(f"فوركس: {len(report['by_market']['forex'])}")
print(f"عملات رقمية: {len(report['by_market']['crypto'])}")
print(f"أسهم: {len(report['by_market']['stocks'])}")
print(f"سلع: {len(report['by_market']['commodities'])}")
```

### السجلات (Logs)
تُطبع جميع العمليات في الكونسول:
- ⏸️ تعليق الصفقات
- ▶️ إعادة تنشيط الصفقات
- 🗑️ حذف الصفقات القديمة
- 🔄 إعادة التعيين الأسبوعية

---

## ⚠️ ملاحظات مهمة

1. **التوقيت**: جميع الأوقات بتوقيت GMT/UTC ما لم يُذكر خلاف ذلك
2. **العملات الرقمية**: لا تُعلّق أبداً (24/7)
3. **الصفقات القديمة**: تُحذف تلقائياً بعد 7 أيام
4. **الفحص الدوري**: كل 4 ساعات للتأكد من التوافق

---

## 🆘 استكشاف الأخطاء

### المشكلة: الصفقات لا تُعلّق
**الحل**: تأكد من تشغيل `trade_scheduler.py` أو `AUTO_SIGNAL_SYSTEM.bat`

### المشكلة: الصفقات لا تُعاد تنشيطها
**الحل**: تحقق من أوقات الأسواق باستخدام `python market_hours.py`

### المشكلة: ملف suspended_trades.json غير موجود
**الحل**: سيُنشأ تلقائياً عند أول عملية تعليق

---

## 📝 التحديثات المستقبلية

- [ ] إضافة أسواق آسيوية (طوكيو، سنغافورة)
- [ ] دعم العطلات الرسمية
- [ ] إشعارات Telegram عند التعليق/إعادة التنشيط
- [ ] لوحة تحكم ويب للمراقبة

---

تم التطبيق في: 26 يناير 2026
