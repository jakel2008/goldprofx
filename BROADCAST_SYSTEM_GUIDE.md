# 📡 نظام البث التلقائي للإشارات - دليل التشغيل

## ✅ تم الإعداد بنجاح!

### 🚀 ملفات التشغيل السريع

#### 1️⃣ **AUTO_SIGNAL_SYSTEM.bat** (الأفضل - النظام الكامل)
```
تشغيل كامل للنظام:
✓ 📊 المحلل التلقائي (يولد الإشارات)
✓ 📡 نظام البث (يرسل الإشارات)
✓ 🤖 بوت التليجرام VIP
```

#### 2️⃣ **START_BROADCASTER.bat** (البث فقط)
```
تشغيل نظام البث التلقائي فقط
✓ يراقب مجلد signals كل دقيقة
✓ يرسل الإشارات الجديدة تلقائياً
```

### 📊 كيف يعمل النظام؟

1. **المحلل** (`auto_pairs_analyzer.py`)
   - يحلل الأزواج (EURUSD, GBPUSD, XAUUSD, BTCUSD)
   - يحفظ الإشارات في مجلد `signals/`

2. **نظام البث** (`signal_broadcaster.py`)
   - يفحص مجلد `signals/` كل 60 ثانية
   - يقرأ الإشارات الجديدة
   - يحسب جودة كل إشارة (0-100)
   - يرسل للمستخدمين حسب خططهم

3. **فلترة الإشارات حسب الخطة**
   ```
   جودة 85+  →  جميع الخطط (Free, Bronze, Silver, Gold, Platinum)
   جودة 70+  →  Bronze وأعلى
   جودة 55+  →  Bronze وأعلى
   جودة 40+  →  Silver وأعلى
   أقل من 40 →  Gold و Platinum فقط
   ```

### 🔧 الإعدادات

**تغيير فترة الفحص:**
افتح `signal_broadcaster.py` وعدل السطر الأخير:
```python
start_auto_broadcaster(check_interval=60)  # 60 = دقيقة واحدة
```

**استثناء إشارة من الإرسال:**
احذف ملف `sent_signals.json` لإعادة إرسال جميع الإشارات

### 📝 الملفات المهمة

| الملف | الوظيفة |
|-------|---------|
| `signal_broadcaster.py` | نظام البث الرئيسي |
| `signals/` | مجلد الإشارات |
| `sent_signals.json` | سجل الإشارات المرسلة |
| `vip_subscriptions.db` | قاعدة بيانات المستخدمين |

### ⚠️ ملاحظات مهمة

1. **البوت يجب أن يكون شغال** لإرسال الإشارات
2. **المستخدمون يجب أن يبدأوا محادثة مع البوت** (`/start`)
3. **الإشارات لا تُرسل مرتين** - يتم تسجيلها في `sent_signals.json`

### 🧪 الاختبار

```powershell
# اختبار سريع لإرسال آخر إشارة
python test_broadcast.py

# تشغيل البث التلقائي
python signal_broadcaster.py
```

### 📊 مراقبة النظام

راقب النوافذ الثلاث:
- **Auto Analyzer**: يظهر التحليلات الجديدة
- **Signal Broadcaster**: يظهر الإشارات المرسلة
- **VIP Bot**: يظهر تفاعلات المستخدمين

---

**تم الإعداد**: 2026-01-26
**الحالة**: ✅ جاهز للتشغيل
