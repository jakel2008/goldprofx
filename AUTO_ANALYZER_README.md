# نظام التحليل التلقائي الشامل

## المميزات

✅ **تحليل تلقائي كامل** - تحليل جميع الأزواج تلقائياً  
✅ **تقرير تفصيلي لكل زوج** - إرسال سيناريو شامل لكل زوج  
✅ **مرة واحدة يومياً** - التحليل يحدث مرة واحدة فقط في اليوم  
✅ **إرسال تلقائي للبوت** - جميع التقارير ترسل تلقائياً  

## التثبيت

### 1. تثبيت المكتبة المطلوبة

```bash
pip install schedule
```

### 2. إضافة الأزواج المراد تحليلها

عدّل قائمة `PAIRS_TO_ANALYZE` في `auto_pairs_analyzer.py`:

```python
PAIRS_TO_ANALYZE = [
    ('EURUSD', '1h'),
    ('GBPUSD', '1h'),
    ('XAUUSD', '1h'),
    # أضف المزيد...
]
```

## الاستخدام

### الخيار 1: تشغيل التحليل يدوياً

```bash
python auto_pairs_analyzer.py
```

### الخيار 2: تشغيل الجدولة التلقائية (مستحسن)

```bash
python daily_scheduler.py
```

جاري التحليل يومياً في الساعة 22:00 UTC (افتتاح السوق)

## البيانات المرسلة لكل زوج

لكل زوج يتم إرسال:

```
📊 تحليل EURUSD (1h)

السعر الحالي: 1.08505

المؤشرات:
RSI: 65.32
MACD: 0.00045

التوصية: شراء قوي

الإشارات:
• شراء - RSI مرتفع

الوقت: 2026-01-23 22:30:45
```

## الملخص النهائي

بعد تحليل جميع الأزواج يتم إرسال ملخص شامل:

```
✅ انتهى التحليل الشامل

إجمالي الأزواج المحللة: 9

التوصيات القوية:
• EURUSD: شراء قوي
• XAUUSD: بيع قوي
```

## ملاحظات مهمة

- **البوت التوكن**: تأكد من ضبط متغيرات البيئة `MM_TELEGRAM_BOT_TOKEN` و `MM_TELEGRAM_CHAT_ID`
- **البيانات**: النظام يستخدم API للحصول على بيانات السعر
- **التحديث**: يمكن تعديل الفترة الزمنية من `1h` إلى أي فترة أخرى

## استكشاف الأخطاء

### لا تُرسل الرسائل
- تحقق من التوكن والـ Chat ID
- اختبر الاتصال: `python test_telegram.py`

### لا يتم التحليل
- تأكد من وجود الاتصال بالإنترنت
- تحقق من توفر API البيانات

### لا يتم التشغيل التلقائي
- تأكد من تشغيل `python daily_scheduler.py` في الخلفية
- استخدم `nohup` أو `screen` على Linux
- استخدم Task Scheduler على Windows
