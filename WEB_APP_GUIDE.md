# 🌐 منصة الويب VIP Signals

تم إنشاء منصة ويب كاملة للبرنامج!

## 🚀 التشغيل السريع

```
نقر مزدوج على: START_WEB_APP.bat
```

ثم افتح المتصفح على:
```
http://localhost:5000
```

---

## 📄 الصفحات المتاحة

### 1. **الرئيسية** (http://localhost:5000/)
- عرض الإحصائيات الرئيسية
- آخر الإشارات
- خطط الاشتراك

### 2. **الإشارات** (http://localhost:5000/signals)
- عرض جميع الإشارات المحفوظة
- معلومات كاملة عن كل إشارة
- مستويات الدخول والخروج

### 3. **الخطط** (http://localhost:5000/plans)
- عرض جميع خطط الاشتراك
- مقارنة تفصيلية بين الخطط
- أزرار الاشتراك

### 4. **لوحة التحكم** (http://localhost:5000/dashboard)
- إحصائيات شاملة
- عدد الإشارات المرسلة
- حالة النظام

---

## 📊 الميزات

✅ واجهة جميلة وسهلة الاستخدام  
✅ تحديث فوري للإشارات والإحصائيات  
✅ عرض تفاصيل الإشارات بشكل واضح  
✅ جودة الإشارات بصرية  
✅ خطط اشتراك قابلة للتخصيص  
✅ لوحة تحكم شاملة  
✅ متوافق مع جميع الأجهزة  

---

## 🔧 التشغيل المتقدم

تشغيل يدوي:
```bash
cd "d:\GOLD PRO"
python web_app.py
```

التشغيل على منفذ مختلف:
```bash
python -c "from web_app import app; app.run(port=8000)"
```

---

## 🌐 الوصول الخارجي

لجعل الموقع متاحاً من أجهزة أخرى في الشبكة:

افتح `web_app.py` وغير السطر الأخير من:
```python
app.run(debug=True, host='0.0.0.0', port=5000)
```

ثم استخدم IP جهازك:
```
http://192.168.1.100:5000
```

---

## 📱 الوصول عبر الهاتف

1. احصل على IP جهازك الكمبيوتر
2. افتح المتصفح على الهاتف
3. اكتب: `http://[IP]:5000`

مثال:
```
http://192.168.1.100:5000
```

---

## 🔗 API Endpoints

```
GET /api/signals      - جلب الإشارات
GET /api/stats        - جلب الإحصائيات
POST /api/subscribe   - الاشتراك
```

---

## ⚙️ التخصيص

### تغيير الألوان
عدّل ملف `templates/base.html` الألوان:
```css
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
```

### إضافة ميزات جديدة
أضف روابط جديدة في `web_app.py`:
```python
@app.route('/page-name')
def page_name():
    return render_template('page-name.html')
```

---

## 🐛 حل المشاكل الشائعة

### "Port already in use"
غير المنفذ:
```bash
python -c "from web_app import app; app.run(port=8000)"
```

### لا تظهر الإشارات
تأكد من أن مجلد `signals/` يحتوي على ملفات JSON

### الصفحة بيضاء
افتح `http://localhost:5000/` بدلاً من `http://127.0.0.1:5000/`

---

**الآن لديك منصة ويب كاملة! 🎉**
