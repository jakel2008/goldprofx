# دليل النظام المركزي الشامل
# MONEY MAKER VIP - Master System Guide

## نظرة عامة

تم إنشاء نظام تشغيل مركزي شامل يدير جميع مكونات MONEY MAKER VIP بشكل احترافي وتلقائي.

---

## 🚀 التشغيل السريع

### الطريقة الأولى: استخدام ملف البدء السريع

```batch
START_MASTER_SYSTEM.bat
```

### الطريقة الثانية: يدوياً

```powershell
# 1. تفعيل البيئة الافتراضية
.\.venv-1\Scripts\Activate.ps1

# 2. تشغيل النظام المركزي
python master_system.py
```

---

## 📋 المكونات المُدارة تلقائياً

### 1. تطبيق الويب (Web App)
- **الملف**: `web_app.py`
- **المنفذ**: 5000
- **الحالة**: يعمل تلقائياً
- **الوصول**: http://localhost:5000

### 2. بوت تليجرام VIP
- **الملف**: `vip_bot_simple.py`
- **الحالة**: يعمل تلقائياً
- **الوظيفة**: استقبال الطلبات والرد على المشتركين

### 3. نظام البث التلقائي
- **الملف**: `signal_broadcaster.py`
- **الفاصل الزمني**: كل 15 دقيقة
- **الوظيفة**: قراءة الإشارات من مجلد signals/ وإرسالها

### 4. المحلل التلقائي
- **الملف**: `auto_pairs_analyzer.py`
- **الفاصل الزمني**: كل 30 دقيقة
- **الأزواج**: EUR/USD, GBP/USD, XAU/USD, BTC/USD, وغيرها

### 5. نظام التقارير
- **الملف**: `periodic_reports.py`
- **الفاصل الزمني**: يومياً عند الساعة 23:00
- **الوظيفة**: إنشاء تقارير شاملة وإرسالها

### 6. تحديث الصفقات النشطة
- **الفاصل الزمني**: كل 5 دقائق
- **الملف**: `active_trades.json`
- **الوظيفة**: متابعة وتحديث حالة الصفقات

---

## ⚙️ ملف الإعدادات

### موقع الملف
```
master_config.json
```

### الإعدادات الرئيسية

```json
{
    "pairs": [
        "EUR/USD",
        "GBP/USD",
        "USD/JPY",
        "XAU/USD",
        "BTC/USD",
        "EUR/GBP",
        "AUD/USD",
        "USD/CAD"
    ],
    "intervals": ["15m", "1h", "4h"],
    "analysis_schedule_minutes": 30,
    "broadcast_schedule_minutes": 15,
    "trades_update_minutes": 5,
    "daily_report_time": "23:00",
    "auto_start": {
        "web_app": true,
        "vip_bot": true,
        "signal_broadcaster": true,
        "auto_analyzer": true
    }
}
```

### تخصيص الإعدادات

#### إضافة أزواج جديدة
```json
"pairs": [
    "EUR/USD",
    "GBP/USD",
    "USD/JPY",
    "EUR/JPY"  // زوج جديد
]
```

#### تغيير فترة التحليل
```json
"analysis_schedule_minutes": 15  // تحليل كل 15 دقيقة بدلاً من 30
```

#### تعطيل مكون معين
```json
"auto_start": {
    "web_app": true,
    "vip_bot": true,
    "signal_broadcaster": false,  // تعطيل البث التلقائي
    "auto_analyzer": true
}
```

---

## 🎯 الجدولة التلقائية

### جدول المهام

| المهمة | التكرار | الوقت |
|-------|---------|-------|
| التحليل التلقائي | كل 30 دقيقة | حسب الإعداد |
| بث الإشارات | كل 15 دقيقة | حسب الإعداد |
| تحديث الصفقات | كل 5 دقائق | حسب الإعداد |
| التقرير اليومي | مرة واحدة يومياً | 23:00 |

### تعديل الجدول

لتعديل أوقات الجدولة، عدّل الإعدادات في `master_config.json`:

```json
{
    "analysis_schedule_minutes": 30,    // تحليل تلقائي
    "broadcast_schedule_minutes": 15,   // بث إشارات
    "trades_update_minutes": 5,         // تحديث صفقات
    "daily_report_time": "23:00"        // وقت التقرير اليومي
}
```

---

## 📊 لوحة التحكم المركزية

### الوصول إلى اللوحة

```
http://localhost:5000/master-dashboard
```

### الميزات

#### 1. **حالة المكونات**
- تطبيق الويب (Web App)
- بوت تليجرام (Telegram Bot)
- نظام البث (Signal Broadcaster)
- المجدول (Scheduler)

#### 2. **المقاييس الرئيسية**
- 👥 إجمالي المشتركين
- 📊 الإشارات المرسلة اليوم
- 📈 الصفقات النشطة
- 📉 معدل النجاح

#### 3. **الصفقات النشطة**
- عرض جميع الصفقات المفتوحة
- معلومات كاملة (الدخول، الهدف، وقف الخسارة)
- حالة كل صفقة

#### 4. **سجل النظام**
- سجل مباشر لجميع العمليات
- تصنيف الرسائل (نجاح، تحذير، خطأ)
- تحديث تلقائي كل 5 ثوانٍ

#### 5. **آخر التحليلات**
- جدول شامل بآخر 20 تحليل
- معلومات الإشارة والدخول والأهداف
- درجة الجودة لكل إشارة

---

## 📁 هيكل الملفات

```
GOLD PRO/
├── master_system.py              # النظام المركزي الرئيسي
├── master_config.json            # ملف الإعدادات
├── START_MASTER_SYSTEM.bat       # ملف البدء السريع
├── system_status.json            # حالة النظام الحالية
├── master_system.log             # سجل النظام
│
├── vip_bot_simple.py             # بوت تليجرام
├── signal_broadcaster.py         # نظام البث
├── auto_pairs_analyzer.py        # المحلل التلقائي
├── periodic_reports.py           # مولد التقارير
├── trade_statistics.py           # إحصائيات الصفقات
├── daily_scheduler.py            # الجدولة اليومية
│
├── web_app.py                    # تطبيق الويب
├── templates/
│   └── master_dashboard.html    # لوحة التحكم المركزية
│
├── signals/                      # مجلد الإشارات
├── active_trades.json            # الصفقات النشطة
├── sent_signals.json             # الإشارات المرسلة
└── vip_subscriptions.db          # قاعدة بيانات المشتركين
```

---

## 🔧 استكشاف الأخطاء

### المشكلة: النظام لا يعمل

**الحل:**
```powershell
# 1. تحقق من البيئة الافتراضية
.\.venv-1\Scripts\Activate.ps1

# 2. تحقق من المكتبات
pip install -r requirements.txt

# 3. أعد تشغيل النظام
python master_system.py
```

### المشكلة: البوت لا يستجيب

**الحل:**
1. تحقق من التوكن في متغيرات البيئة
2. تأكد من `vip_bot_simple.py` يعمل
3. راجع ملف `master_system.log`

### المشكلة: لا توجد إشارات

**الحل:**
1. تحقق من مجلد `signals/`
2. راجع ملف `master_config.json`
3. تأكد من عمل `auto_pairs_analyzer.py`

### المشكلة: لوحة التحكم لا تعمل

**الحل:**
```powershell
# 1. تحقق من web_app.py
python web_app.py

# 2. افتح المتصفح على
http://localhost:5000/master-dashboard
```

---

## 📝 السجلات

### ملف السجل الرئيسي
```
master_system.log
```

### قراءة السجلات

```powershell
# عرض آخر 50 سطر
Get-Content -Path "master_system.log" -Tail 50

# متابعة السجلات مباشرة
Get-Content -Path "master_system.log" -Wait -Tail 10
```

### أنواع السجلات

| الرمز | النوع | الوصف |
|------|-------|-------|
| ℹ️ | INFO | معلومات عامة |
| ✅ | SUCCESS | عملية ناجحة |
| ⚠️ | WARNING | تحذير |
| ❌ | ERROR | خطأ |

---

## 🎛️ التحكم المتقدم

### إيقاف النظام

```
Ctrl + C  في نافذة PowerShell
```

أو

```powershell
# إيقاف جميع عمليات Python
Stop-Process -Name "python" -Force
```

### إعادة تشغيل مكون معين

```powershell
# مثلاً إعادة تشغيل البوت فقط
python vip_bot_simple.py
```

### التشغيل في الخلفية

```powershell
# تشغيل كخدمة Windows (متقدم)
# يتطلب إعداد إضافي
```

---

## 📊 إدارة المشتركين

### عرض جميع المشتركين

```powershell
python -c "from vip_subscription_system import SubscriptionManager; sm = SubscriptionManager(); print(sm.get_all_active_users())"
```

### إضافة مشترك جديد

```powershell
python ADD_USER_SIMPLE.py
```

### ترقية مشترك

```powershell
python ADMIN_PANEL.bat
```

---

## 🔐 الأمان

### تغيير مفتاح الجلسة

في `web_app.py`:
```python
app.secret_key = 'your-new-secure-random-key-here'
```

### حماية المتغيرات البيئية

```powershell
# ضبط التوكن
$env:MM_TELEGRAM_BOT_TOKEN = "your-token-here"
$env:MM_TELEGRAM_CHAT_ID = "your-chat-id-here"
```

---

## 📈 مراقبة الأداء

### فحص استهلاك الموارد

```powershell
# استهلاك CPU والذاكرة
Get-Process python | Select-Object Name, CPU, @{Name="Memory(MB)";Expression={$_.WS / 1MB}}
```

### عدد الإشارات المرسلة

```powershell
# قراءة ملف الإشارات المرسلة
Get-Content sent_signals.json | ConvertFrom-Json | Measure-Object
```

---

## 🆘 الدعم

### الملفات المهمة للدعم

عند طلب الدعم، أرسل:

1. `master_system.log` - سجل النظام
2. `master_config.json` - الإعدادات
3. `system_status.json` - حالة النظام
4. لقطة شاشة من لوحة التحكم

### معلومات النظام

```powershell
# معلومات Python
python --version

# معلومات المكتبات
pip list | Select-String "requests|flask|schedule|pandas"
```

---

## ✅ قائمة التحقق

قبل البدء، تأكد من:

- [ ] تفعيل البيئة الافتراضية
- [ ] تثبيت جميع المكتبات
- [ ] ضبط التوكن ومعرف الدردشة
- [ ] وجود ملف `master_config.json`
- [ ] صلاحيات الكتابة على المجلد
- [ ] اتصال بالإنترنت

---

## 🎯 النصائح

### للأداء الأمثل

1. **استخدم SSD** لتخزين البيانات
2. **احتفظ بنسخة احتياطية** من قاعدة البيانات يومياً
3. **راقب السجلات** بانتظام
4. **نظّف الملفات القديمة** من مجلد signals/ أسبوعياً
5. **حدّث المكتبات** شهرياً

### للاستقرار

1. استخدم **إعدادات محافظة** في البداية
2. اختبر على **بيانات تجريبية** أولاً
3. راقب **استهلاك الذاكرة** بانتظام
4. احتفظ بـ **نسخة احتياطية** من الإعدادات

---

## 📞 جهات الاتصال

- **التليجرام**: @YourSupportBot
- **البريد الإلكتروني**: support@moneymaker.vip
- **الموقع**: https://moneymaker.vip

---

**تم إنشاء هذا الدليل بواسطة MONEY MAKER VIP Team**
**آخر تحديث**: 2025-01-24
